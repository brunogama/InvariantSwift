---
phase: 06-documentation-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/XCTEST_MIGRATION.md
  - docs/examples/sorting.md
  - docs/examples/json_codable.md
  - docs/examples/collections.md
autonomous: true

must_haves:
  truths:
    - "Developer can see XCTest code and equivalent property test side-by-side"
    - "20 migration examples cover common testing patterns"
    - "All code examples compile and pass"
    - "Examples use InvariantSwift v2.0 features (cover, classify, label)"
  artifacts:
    - path: "docs/XCTEST_MIGRATION.md"
      provides: "Complete migration guide with 20 before/after examples"
      min_lines: 400
    - path: "docs/examples/sorting.md"
      provides: "Sorting algorithm testing patterns"
      min_lines: 80
    - path: "docs/examples/json_codable.md"
      provides: "JSON encode/decode roundtrip patterns"
      min_lines: 80
    - path: "docs/examples/collections.md"
      provides: "Collection testing patterns"
      min_lines: 80
  key_links:
    - from: "docs/XCTEST_MIGRATION.md"
      to: "docs/COOKBOOK.md"
      via: "cross-references"
      pattern: "See also.*COOKBOOK"
    - from: "docs/examples/*.md"
      to: "docs/XCTEST_MIGRATION.md"
      via: "backlinks"
      pattern: "Migration Guide"
---

<objective>
Create a comprehensive XCTest migration guide with 20 before/after examples showing how to convert traditional unit tests to property-based tests using InvariantSwift v2.0.

Purpose: Enable XCTest users to adopt property-based testing with clear, actionable migration paths.
Output: `docs/XCTEST_MIGRATION.md` and 3 domain-specific example files demonstrating real-world patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-examples/06-RESEARCH.md

# Existing documentation structure
@docs/COOKBOOK.md
@docs/ONBOARDING.md
@docs/QUICKSTART.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XCTest Migration Guide</name>
  <files>docs/XCTEST_MIGRATION.md</files>
  <action>
Create `docs/XCTEST_MIGRATION.md` with the following structure:

1. **Introduction** (why migrate, benefits of property-based testing)
   - No FP jargon - explain in XCTest terms
   - Clear value proposition: "test all inputs, not just examples"

2. **Quick Migration Checklist** (5-minute overview)
   - Import InvariantSwift
   - Add @PropertyTest macro
   - Replace XCTAssert with #expect
   - Add classification for coverage verification

3. **Before/After Examples** (20 total, organized by category):

   **Basic Patterns (5 examples):**
   1. Single value assertion -> property test
   2. Multiple assertions -> single property with multiple #expect
   3. Test fixtures -> generators
   4. Test setup/teardown -> generator composition
   5. Parameterized tests -> @PropertyTest with multiple params

   **Data Structure Patterns (5 examples):**
   6. Array manipulation (sorting, filtering)
   7. Dictionary operations (insert, lookup, remove)
   8. Set operations (union, intersection)
   9. Stack/Queue invariants
   10. Tree traversal properties

   **Serialization Patterns (3 examples):**
   11. JSON encode/decode roundtrip
   12. Codable with optional fields
   13. Custom encoder/decoder

   **Business Logic Patterns (4 examples):**
   14. Validation rules
   15. State machine transitions
   16. Price calculations
   17. Date/time operations

   **Edge Cases Patterns (3 examples):**
   18. Empty collections
   19. Boundary values
   20. Unicode/special characters

4. **Using v2.0 Features** (show each example with cover/classify/label):
   - Add `.cover()` to enforce coverage requirements
   - Add `.classify()` to track input distributions
   - Add `.label()` for readable test output

5. **Incremental Migration Strategy**
   - Start with pure functions
   - Then data structures
   - Then business logic
   - Keep XCTest for integration tests

6. **Troubleshooting Common Issues**
   - "How do I handle async code?"
   - "What about mocking?"
   - "How do I test side effects?"

Format: Use clear Markdown with code blocks, tables for comparison, and cross-references to COOKBOOK.md.

DO NOT use FP terminology (functor, monad, applicative). Use Swift-native language.
  </action>
  <verify>
    - File exists at docs/XCTEST_MIGRATION.md
    - Contains exactly 20 before/after examples
    - No FP jargon: `rg "monad|functor|applicative|monoid" docs/XCTEST_MIGRATION.md` returns empty
    - Cross-references to COOKBOOK.md present
    - All code blocks have `swift` language tag
  </verify>
  <done>
    - Migration guide with 20 examples created
    - Each example shows XCTest "before" and InvariantSwift "after"
    - v2.0 features (cover, classify, label) demonstrated
    - No FP terminology in user-facing content
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Domain Example Files</name>
  <files>docs/examples/sorting.md, docs/examples/json_codable.md, docs/examples/collections.md</files>
  <action>
Create three domain-specific example files using the recipe pattern from 06-RESEARCH.md:

**docs/examples/sorting.md:**
- Recipe: Testing sort correctness (idempotent, ordered, permutation)
- Recipe: Testing sort stability
- Recipe: Testing custom comparators
- Recipe: Testing partial sorting
- Each recipe: Problem -> Solution -> Discussion -> Related Recipes

**docs/examples/json_codable.md:**
- Recipe: Basic Codable roundtrip
- Recipe: Nested type encoding
- Recipe: Optional field handling
- Recipe: Custom coding keys
- Recipe: Date encoding strategies

**docs/examples/collections.md:**
- Recipe: Array invariants (append, insert, remove)
- Recipe: Dictionary invariants (get/set/delete)
- Recipe: Set algebra laws (union, intersection, difference)
- Recipe: Collection transformations (map, filter, reduce properties)

Each file structure:
```markdown
# [Domain] Property Testing Patterns

Practical recipes for testing [domain] with InvariantSwift.

## Prerequisites
- InvariantSwift v2.0
- Swift 6.0+
- Understanding of @PropertyTest macro

## Recipes

### Recipe: [Name]

**Problem:** [What testing challenge this solves]

**Solution:**
[Code block with complete, runnable example using @PropertyTest]

**Discussion:**
[Why this property matters, what edge cases it catches]

**With Classification (v2.0):**
[Same example with .cover(), .classify(), .label()]

**Related Recipes:**
[Links to other relevant recipes]
```

All code examples must:
- Use @PropertyTest macro syntax
- Include v2.0 classification features
- Be complete and runnable
- Avoid FP jargon
  </action>
  <verify>
    - Three files exist in docs/examples/
    - Each file has at least 4 recipes
    - All recipes follow Problem -> Solution -> Discussion structure
    - No FP jargon: `rg "monad|functor|applicative" docs/examples/` returns empty
  </verify>
  <done>
    - docs/examples/sorting.md with 4+ recipes
    - docs/examples/json_codable.md with 5+ recipes
    - docs/examples/collections.md with 4+ recipes
    - All examples use v2.0 features
    - Recipe pattern consistently applied
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Cross-References and Validate</name>
  <files>docs/XCTEST_MIGRATION.md, docs/examples/*.md</files>
  <action>
1. Add cross-references between all documentation:
   - XCTEST_MIGRATION.md links to relevant COOKBOOK.md sections
   - XCTEST_MIGRATION.md links to domain example files
   - Domain example files link back to migration guide
   - Add "See also" sections at end of each file

2. Create table of contents with anchor links in XCTEST_MIGRATION.md

3. Validate all internal links resolve correctly

4. Ensure consistent formatting:
   - Code blocks use `swift` language tag
   - Tables have consistent column widths
   - Headers follow hierarchy (# -> ## -> ###)

5. Add footer with version info:
   ```markdown
   ---
   *InvariantSwift v2.0 | [COOKBOOK](COOKBOOK.md) | [API Reference](API_REFERENCE_GENERATED.md)*
   ```
  </action>
  <verify>
    - All internal links valid (no broken references)
    - Consistent header hierarchy in all files
    - Footer present in all new files
    - Table of contents matches actual sections
  </verify>
  <done>
    - Cross-references complete and validated
    - All links resolve
    - Consistent formatting throughout
    - Files ready for review
  </done>
</task>

</tasks>

<verification>
Manual verification:
1. Read through XCTEST_MIGRATION.md - should be accessible to XCTest-only developers
2. Verify 20 examples cover diverse testing scenarios
3. Check that v2.0 features (cover, classify, label) are demonstrated
4. Confirm no FP jargon in any user-facing content

Automated verification:
```bash
# Count examples
rg -c "### Before|### After" docs/XCTEST_MIGRATION.md

# Check for FP jargon
rg "monad|functor|applicative|monoid|semigroup" docs/XCTEST_MIGRATION.md docs/examples/

# Verify file sizes (minimum content)
wc -l docs/XCTEST_MIGRATION.md  # Should be > 400 lines
wc -l docs/examples/*.md         # Each should be > 80 lines
```
</verification>

<success_criteria>
- [ ] docs/XCTEST_MIGRATION.md exists with 20 before/after examples
- [ ] docs/examples/sorting.md exists with 4+ recipes
- [ ] docs/examples/json_codable.md exists with 5+ recipes
- [ ] docs/examples/collections.md exists with 4+ recipes
- [ ] All code examples use @PropertyTest macro
- [ ] v2.0 features (cover, classify, label) demonstrated
- [ ] No FP jargon in user-facing content
- [ ] Cross-references complete and valid
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-examples/06-01-SUMMARY.md`
</output>
