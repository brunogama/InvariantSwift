---
phase: 04.1-refactor-fakery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/InvariantSwiftDomainGenerators/Data/DataType.swift
  - Sources/InvariantSwiftDomainGenerators/Data/DataLocale.swift
  - Sources/InvariantSwiftDomainGenerators/Data/DomainDataStore.swift
  - Sources/InvariantSwiftDomainGenerators/Data/DomainDataGenerator.swift
  - Sources/InvariantSwiftDomainGenerators/DataGenerators.swift
autonomous: true

must_haves:
  truths:
    - "Gen.domainData(.email) generates valid email addresses"
    - "Gen.domainData(.firstName, locale: .ptBR) generates Brazilian Portuguese names"
    - "DataType enum contains all 180+ cases from FakerType"
    - "DataLocale enum contains all 25+ locales"
    - "Gen.fake.name.firstName continues to work (fluent API unchanged)"
  artifacts:
    - path: "Sources/InvariantSwiftDomainGenerators/Data/DataType.swift"
      provides: "DataType enum with all data categories"
      contains: "public enum DataType"
    - path: "Sources/InvariantSwiftDomainGenerators/Data/DataLocale.swift"
      provides: "DataLocale enum with locale definitions"
      contains: "public enum DataLocale"
    - path: "Sources/InvariantSwiftDomainGenerators/Data/DomainDataStore.swift"
      provides: "Thread-safe locale data storage"
      contains: "public final class DomainDataStore"
    - path: "Sources/InvariantSwiftDomainGenerators/Data/DomainDataGenerator.swift"
      provides: "Gen.domainData() generator extension"
      contains: "public static func domainData"
    - path: "Sources/InvariantSwiftDomainGenerators/DataGenerators.swift"
      provides: "Fluent API (Gen.fake namespace)"
      contains: "public static var fake"
  key_links:
    - from: "DomainDataGenerator.swift"
      to: "DomainDataStore.shared"
      via: "Locale data access"
      pattern: "DomainDataStore\\.shared\\.data"
    - from: "DomainDataGenerator.swift"
      to: "DataType"
      via: "Switch statement"
      pattern: "switch type"
    - from: "DataGenerators.swift"
      to: "DomainDataGenerator"
      via: "Gen.domainData calls"
      pattern: "Gen\\.domainData"
---

<objective>
Rename Faker types and files to vendor-neutral Domain Data terminology.

Purpose: Remove vendor-specific naming (Faker/Fakery) that suggests external library dependency when InvariantSwift's implementation is fully self-contained.

Output: Renamed module structure with `DataType`, `DataLocale`, `DomainDataStore`, and `Gen.domainData()` public API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-refactor-fakery-to-domain-data/04.1-RESEARCH.md

# Existing files to rename
@Sources/InvariantSwiftDomainGenerators/Faker/FakerType.swift
@Sources/InvariantSwiftDomainGenerators/Faker/FakerLocale.swift
@Sources/InvariantSwiftDomainGenerators/Faker/FakerData.swift
@Sources/InvariantSwiftDomainGenerators/Faker/FakerGenerator.swift
@Sources/InvariantSwiftDomainGenerators/FakeryGenerators.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directory and type definition files</name>
  <files>
    Sources/InvariantSwiftDomainGenerators/Data/DataType.swift
    Sources/InvariantSwiftDomainGenerators/Data/DataLocale.swift
    Sources/InvariantSwiftDomainGenerators/Data/DomainDataStore.swift
  </files>
  <action>
    Rename the Faker/ directory to Data/ and rename files:

    1. Create Data/ directory: `mkdir -p Sources/InvariantSwiftDomainGenerators/Data`

    2. Rename FakerType.swift to DataType.swift:
       - Copy file to new location
       - Replace `public enum FakerType` with `public enum DataType`
       - Update all internal references from FakerType to DataType
       - Keep all 180+ enum cases exactly as they are
       - Update doc comments to remove "Faker" terminology

    3. Rename FakerLocale.swift to DataLocale.swift:
       - Copy file to new location
       - Replace `public enum FakerLocale` with `public enum DataLocale`
       - Keep `static var default: DataLocale { .enUS }` as is
       - Keep all 25+ locale cases exactly as they are
       - Update internal references

    4. Rename FakerData.swift to DomainDataStore.swift:
       - Copy file to new location
       - Replace `public final class FakerData` with `public final class DomainDataStore`
       - Update parameter types from FakerLocale to DataLocale
       - Replace `loadedLocales: [FakerLocale: LocaleData]` with `loadedLocales: [DataLocale: LocaleData]`
       - Keep all locale data arrays (firstNames, lastNames, cities, etc.) unchanged

    Do NOT delete original files yet (needed for deprecated aliases in Plan 02).
  </action>
  <verify>
    All three new files exist:
    - `ls Sources/InvariantSwiftDomainGenerators/Data/DataType.swift`
    - `ls Sources/InvariantSwiftDomainGenerators/Data/DataLocale.swift`
    - `ls Sources/InvariantSwiftDomainGenerators/Data/DomainDataStore.swift`

    Types are properly renamed (grep confirms):
    - `rg "public enum DataType" Sources/`
    - `rg "public enum DataLocale" Sources/`
    - `rg "public final class DomainDataStore" Sources/`
  </verify>
  <done>
    DataType, DataLocale, and DomainDataStore types exist in Data/ directory with correct naming.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename generator files and add Gen.domainData API</name>
  <files>
    Sources/InvariantSwiftDomainGenerators/Data/DomainDataGenerator.swift
    Sources/InvariantSwiftDomainGenerators/DataGenerators.swift
  </files>
  <action>
    Rename generator files with new public API:

    1. Rename FakerGenerator.swift to DomainDataGenerator.swift:
       - Copy file to Data/DomainDataGenerator.swift
       - Replace `public static func faker(_ type: FakerType, locale: FakerLocale = .enUS)` with:
         `public static func domainData(_ type: DataType, locale: DataLocale = .enUS)`
       - Update internal references: FakerType -> DataType, FakerLocale -> DataLocale
       - Replace `FakerData.shared` with `DomainDataStore.shared`
       - Keep all switch case implementations unchanged (they generate the same data)

    2. Rename FakeryGenerators.swift to DataGenerators.swift:
       - Copy file to new location (same directory level)
       - Keep `public static var fake: FakeGenerators` as is (fluent API stays same)
       - Update internal generator calls from `Gen.faker(.xxx)` to `Gen.domainData(.xxx)`
       - Update type references: FakerType -> DataType, FakerLocale -> DataLocale
       - Keep FakeGenerators struct name (it's the public API, "fake data" is vendor-neutral)
       - Keep all nested generator structs (NameGenerators, AddressGenerators, etc.)

    Do NOT delete original files yet (needed for deprecated aliases in Plan 02).
  </action>
  <verify>
    New files exist and contain correct API:
    - `ls Sources/InvariantSwiftDomainGenerators/Data/DomainDataGenerator.swift`
    - `ls Sources/InvariantSwiftDomainGenerators/DataGenerators.swift`
    - `rg "public static func domainData" Sources/`
    - `rg "Gen\.domainData" Sources/InvariantSwiftDomainGenerators/DataGenerators.swift`
  </verify>
  <done>
    Gen.domainData() API exists and DataGenerators.swift uses it internally.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verification and fix any compilation errors</name>
  <files>
    Sources/InvariantSwiftDomainGenerators/Data/*.swift
    Sources/InvariantSwiftDomainGenerators/DataGenerators.swift
  </files>
  <action>
    Verify the renamed module compiles and fix any issues:

    1. Run build to check for compilation errors:
       `swift build 2>&1 | head -50`

    2. Common issues to fix:
       - Missing imports: Add `import Foundation` if needed
       - Circular references: Ensure DomainDataStore doesn't reference DomainDataGenerator
       - Type mismatches: Verify all FakerType -> DataType, FakerLocale -> DataLocale renames complete
       - Access level: Ensure all public types remain public

    3. Fix any SwiftLint warnings:
       - Run `swiftlint lint --path Sources/InvariantSwiftDomainGenerators/Data/ 2>&1 | head -30`
       - Fix identifier naming, line length, or other violations

    4. Run format on new files:
       `swift-format -i --configuration .swift-format Sources/InvariantSwiftDomainGenerators/Data/*.swift Sources/InvariantSwiftDomainGenerators/DataGenerators.swift`

    The build may still show warnings from OLD Faker files - that's expected until Plan 02 adds deprecated aliases.
  </action>
  <verify>
    Build succeeds for new files (ignore warnings from old Faker files):
    - `swift build` completes without errors in Data/*.swift or DataGenerators.swift
    - `swiftlint lint --strict --path Sources/InvariantSwiftDomainGenerators/Data/` reports zero violations
  </verify>
  <done>
    New domain data module compiles successfully with zero SwiftLint violations in new files.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Directory structure: `ls Sources/InvariantSwiftDomainGenerators/Data/` shows 4 files
2. New API exists: `rg "public static func domainData" Sources/` finds DomainDataGenerator.swift
3. Types renamed: `rg "public enum DataType|public enum DataLocale|public final class DomainDataStore" Sources/` finds 3 matches
4. Build passes: `swift build` completes (warnings from old Faker files expected until Plan 02)
5. Old files still exist: `ls Sources/InvariantSwiftDomainGenerators/Faker/` shows original files (for deprecated aliases)
</verification>

<success_criteria>
- [ ] Data/ directory created with 4 renamed files
- [ ] DataType enum contains all 180+ cases from FakerType
- [ ] DataLocale enum contains all 25+ locales from FakerLocale
- [ ] DomainDataStore class provides thread-safe locale data access
- [ ] Gen.domainData() API implemented with same functionality as Gen.faker()
- [ ] DataGenerators.swift uses Gen.domainData() internally for Gen.fake namespace
- [ ] Zero SwiftLint violations in new files
- [ ] Build succeeds for new files (old file warnings acceptable)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-refactor-fakery-to-domain-data/04.1-01-SUMMARY.md`
</output>
