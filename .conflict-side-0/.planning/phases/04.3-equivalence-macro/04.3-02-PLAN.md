---
phase: 04.3-equivalence-macro
plan: 02
type: execute
wave: 2
depends_on: ["04.3-01"]
files_modified:
  - Tests/InvariantSwiftMacroTests/EquivalenceMacroTests.swift
  - docs/MACROS.md
autonomous: true

must_haves:
  truths:
    - "Tests verify macro expansion produces correct Swift Testing code"
    - "Tests verify tolerance parameter generates floating-point comparison"
    - "Tests verify error diagnostics for invalid usage"
    - "Documentation shows common use cases with examples"
    - "All tests pass with swift test"
  artifacts:
    - path: "Tests/InvariantSwiftMacroTests/EquivalenceMacroTests.swift"
      provides: "Comprehensive macro expansion tests"
      min_lines: 100
    - path: "docs/MACROS.md"
      provides: "Updated documentation with @Equivalence section"
      contains: "@Equivalence"
  key_links:
    - from: "Tests/InvariantSwiftMacroTests/EquivalenceMacroTests.swift"
      to: "Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceMacro.swift"
      via: "test import"
      pattern: "@testable import InvariantSwiftMacros"
---

<objective>
Create comprehensive tests and documentation for the @Equivalence macro.

Purpose: Verify macro expansion correctness and provide developer guidance for function equivalence testing.

Output: Test suite validating all macro behaviors, updated MACROS.md documentation with usage examples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-equivalence-macro/04.3-RESEARCH.md
@.planning/phases/04.3-equivalence-macro/04.3-01-SUMMARY.md
@Tests/InvariantSwiftMacroTests/PropertyMacroTests.swift
@docs/MACROS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EquivalenceMacroTests.swift</name>
  <files>
    Tests/InvariantSwiftMacroTests/EquivalenceMacroTests.swift
  </files>
  <action>
    Create comprehensive macro expansion tests following PropertyMacroTests.swift pattern:

    1. Import structure (follow PropertyMacroTests.swift exactly):
       - import SwiftSyntax
       - import SwiftSyntaxBuilder
       - import SwiftSyntaxMacros
       - import SwiftSyntaxMacrosTestSupport
       - import Testing
       - import InvariantSwiftCore (for core types)
       - @testable import InvariantSwiftMacros

       NOTE: The test file verifies MACRO EXPANSION, not runtime behavior.
       FloatingPointTolerance appears in the EXPANDED CODE, but tests only verify
       the expansion STRING contains "FloatingPointTolerance.absolute". The macro
       test target does not need to import InvariantSwift - it only checks string
       output of macro expansion.

    2. Create EquivalenceMacroTests struct with @Suite annotation

    3. Test categories:

    **Basic Expansion Tests:**
    - @Test("Basic equivalence test generates wrapper enum")
      Verify: @Equivalence applied to func with (reference: F, candidate: F) generates enum with @Test
    - @Test("Generated test calls both functions and compares results")
      Verify: Expansion contains reference(input), candidate(input), comparison
    - @Test("Custom iterations parameter is respected")
      Verify: @Equivalence(iterations: 1000) uses 1000 in generated code

    **Tolerance Tests:**
    - @Test("Tolerance parameter generates floating-point comparison")
      Verify: @Equivalence(tolerance: 0.0001) expansion contains "FloatingPointTolerance.absolute"
    - @Test("Nil tolerance uses Equatable comparison")
      Verify: Without tolerance, generates != comparison

    **Error Diagnostic Tests:**
    - @Test("Error: applied to non-function")
      Verify: @Equivalence on var/struct emits mustBeFunction diagnostic
    - @Test("Error: function with wrong parameter count")
      Verify: func test(x: Int) emits requiresTwoFunctionParameters
    - @Test("Error: function with no parameters")
      Verify: func test() emits requiresTwoFunctionParameters
    - @Test("Error: tolerance on non-floating-point type")
      Verify: @Equivalence(tolerance: 0.1) on func returning Int emits toleranceRequiresBinaryFloatingPoint

    **Edge Case Tests:**
    - @Test("Async function generates async test")
      Verify: async func generates async throws test function
    - @Test("Throwing functions are handled correctly")
      Verify: throws func generates proper try/catch handling

    4. Use MacroTestCase helper pattern from PropertyMacroTests for assertions
    5. Test against testMacros dictionary containing EquivalenceMacro.self
    6. Follow Swift Testing format with @Test and #expect

    Total: 11+ tests covering all macro behaviors including tolerance validation
  </action>
  <verify>
    swift test --filter EquivalenceMacroTests 2>&1 | head -100
    swiftlint lint --strict Tests/InvariantSwiftMacroTests/EquivalenceMacroTests.swift
    wc -l Tests/InvariantSwiftMacroTests/EquivalenceMacroTests.swift
  </verify>
  <done>
    - EquivalenceMacroTests.swift exists with 11+ tests
    - Tests cover basic expansion, tolerance, error diagnostics (including tolerance type validation), edge cases
    - All tests pass with swift test
    - SwiftLint reports zero violations
    - Tests follow PropertyMacroTests.swift pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/MACROS.md with @Equivalence documentation</name>
  <files>
    docs/MACROS.md
  </files>
  <action>
    Read existing docs/MACROS.md and add @Equivalence section following the existing pattern:

    1. Find appropriate location (after @PropertyTest or @DifferentialTest section)

    2. Add new section with:
       - Purpose: Safe refactoring, algorithm comparison, migration validation
       - Basic usage with code examples
       - Floating-point comparison with tolerance parameter
       - Parameters table (iterations, tolerance)
       - Generated code explanation
       - Error handling (including tolerance type validation)
       - See also references

    3. Ensure consistent formatting with existing sections
    4. Add @Equivalence to table of contents if one exists
  </action>
  <verify>
    rg "@Equivalence" docs/MACROS.md | head -20
    rg "tolerance" docs/MACROS.md | head -10
  </verify>
  <done>
    - docs/MACROS.md contains @Equivalence section
    - Documentation shows basic usage, tolerance usage, parameters
    - Examples are clear and practical
    - Formatting consistent with existing sections
  </done>
</task>

<task type="auto">
  <name>Task 3: Final verification and build check</name>
  <files>
    (no new files - verification only)
  </files>
  <action>
    Run comprehensive verification:

    1. Full build with warnings-as-errors:
       swift build -Xswiftc -warnings-as-errors

    2. Run all macro tests:
       swift test --filter InvariantSwiftMacroTests

    3. Run EquivalenceMacro tests specifically:
       swift test --filter EquivalenceMacroTests

    4. Lint check all modified files:
       swiftlint lint --strict Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift
       swiftlint lint --strict Sources/InvariantSwiftMacros/EquivalenceMacro/
       swiftlint lint --strict Tests/InvariantSwiftMacroTests/EquivalenceMacroTests.swift

    5. Verify documentation renders correctly (check for markdown errors)

    Fix any issues discovered during verification.
  </action>
  <verify>
    swift build -Xswiftc -warnings-as-errors
    swift test --filter EquivalenceMacroTests 2>&1 | tail -20
  </verify>
  <done>
    - Full build passes with zero warnings
    - All EquivalenceMacro tests pass
    - All InvariantSwiftMacroTests pass
    - SwiftLint reports zero violations
    - Documentation is complete and accurate
    - Phase 04.3 objectives fully met
  </done>
</task>

</tasks>

<verification>
1. Test execution:
   swift test --filter EquivalenceMacroTests

2. Full test suite:
   swift test --filter InvariantSwiftMacroTests

3. Build verification:
   swift build -Xswiftc -warnings-as-errors

4. Lint verification:
   swiftlint lint --strict Tests/InvariantSwiftMacroTests/EquivalenceMacroTests.swift

5. Documentation verification:
   rg "@Equivalence" docs/MACROS.md
</verification>

<success_criteria>
- EquivalenceMacroTests.swift contains 11+ comprehensive tests
- All tests pass with swift test
- docs/MACROS.md contains @Equivalence section with examples
- Full build passes with zero warnings
- SwiftLint reports zero violations
- Phase 04.3 complete: @Equivalence macro fully implemented and documented
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-equivalence-macro/04.3-02-SUMMARY.md`
</output>
