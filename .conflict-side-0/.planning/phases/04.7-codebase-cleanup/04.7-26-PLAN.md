---
phase: 04.7-codebase-cleanup
plan: 26
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/InvariantSwift/Testing/ConfigBuilder.swift
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "ConfigBuilder.swift passes swiftlint lint --strict with zero violations"
    - "All return types use Self instead of ConfigBuilder<T>"
    - "No unneeded synthesized initializer exists"
    - "All lines are 100 characters or less"
  artifacts:
    - path: "Sources/InvariantSwift/Testing/ConfigBuilder.swift"
      provides: "Clean ConfigBuilder pattern implementation"
      contains: "Self(config:"
  key_links:
    - from: "Sources/InvariantSwift/Testing/ConfigBuilder.swift"
      to: "swiftlint"
      via: "lint --strict"
      pattern: "zero violations"
---

<objective>
Fix 5 SwiftLint violations in ConfigBuilder.swift introduced by plan 04.7-21

Purpose: Unblock phase 04.7 completion by achieving zero SwiftLint violations in strict mode
Output: Clean ConfigBuilder.swift that passes `swiftlint lint --strict`
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Sources/InvariantSwift/Testing/ConfigBuilder.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 5 SwiftLint violations in ConfigBuilder.swift</name>
  <files>Sources/InvariantSwift/Testing/ConfigBuilder.swift</files>
  <action>
Apply exactly these 5 fixes to ConfigBuilder.swift:

**Fix 1: Remove unneeded synthesized initializer (line 29)**
Delete the private init entirely - Swift synthesizes memberwise initializers automatically for structs:
```swift
// DELETE these 3 lines:
private init(config: T) {
  self.config = config
}
```

**Fix 2: Use Self in static `from` method (line 42-43)**
Change return type and constructor:
```swift
// Before:
public static func from(_ initial: T) -> ConfigBuilder<T> {
  ConfigBuilder(config: initial)
}

// After:
public static func from(_ initial: T) -> Self {
  Self(config: initial)
}
```

**Fix 3: Fix line length AND use Self in `set` method (lines 57, 60)**
Break line to stay under 100 chars AND use Self:
```swift
// Before (line 57 is 101 chars):
public func set<Value>(_ keyPath: WritableKeyPath<T, Value>, to value: Value) -> ConfigBuilder<T> {
  var updated = config
  updated[keyPath: keyPath] = value
  return ConfigBuilder(config: updated)
}

// After:
public func set<Value>(
  _ keyPath: WritableKeyPath<T, Value>,
  to value: Value
) -> Self {
  var updated = config
  updated[keyPath: keyPath] = value
  return Self(config: updated)
}
```

**Fix 4: Use Self in `update` method (line 77, 81)**
Change return type and constructor:
```swift
// Before:
public func update<Value>(
  _ keyPath: WritableKeyPath<T, Value>,
  _ transform: (Value) -> Value
) -> ConfigBuilder<T> {
  var updated = config
  let currentValue = updated[keyPath: keyPath]
  updated[keyPath: keyPath] = transform(currentValue)
  return ConfigBuilder(config: updated)
}

// After:
public func update<Value>(
  _ keyPath: WritableKeyPath<T, Value>,
  _ transform: (Value) -> Value
) -> Self {
  var updated = config
  let currentValue = updated[keyPath: keyPath]
  updated[keyPath: keyPath] = transform(currentValue)
  return Self(config: updated)
}
```

**Summary of changes:**
- Line 29-31: DELETE private init (3 lines removed)
- Line 42: Return type `ConfigBuilder<T>` -> `Self`
- Line 43: `ConfigBuilder(config:` -> `Self(config:`
- Line 57: Break into 4 lines with return type `Self`
- Line 60: `ConfigBuilder(config:` -> `Self(config:`
- Line 77: Return type `ConfigBuilder<T>` -> `Self`
- Line 81: `ConfigBuilder(config:` -> `Self(config:`
  </action>
  <verify>
Run: `swiftlint lint --strict Sources/InvariantSwift/Testing/ConfigBuilder.swift`
Expected: "Done linting! Found 0 violations, 0 serious in 1 file."
  </verify>
  <done>
ConfigBuilder.swift has zero SwiftLint violations in strict mode
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build and tests pass</name>
  <files>Sources/InvariantSwift/Testing/ConfigBuilder.swift</files>
  <action>
1. Build the project with warnings-as-errors to ensure changes are correct:
   ```bash
   swift build -Xswiftc -warnings-as-errors
   ```

2. Run the LensSystemTests to ensure ConfigBuilder still works correctly:
   ```bash
   swift test --filter LensSystemTests
   ```

3. Run full lint in strict mode to confirm no regressions:
   ```bash
   swiftlint lint --strict Sources/InvariantSwift/
   ```
  </action>
  <verify>
All three commands must pass:
- `swift build -Xswiftc -warnings-as-errors` exits 0
- `swift test --filter LensSystemTests` shows 12/12 tests pass
- `swiftlint lint --strict Sources/InvariantSwift/` shows zero errors
  </verify>
  <done>
ConfigBuilder.swift is verified to be correct, tested, and lint-clean
  </done>
</task>

</tasks>

<verification>
1. `swiftlint lint --strict Sources/InvariantSwift/Testing/ConfigBuilder.swift` reports 0 violations
2. `swift build -Xswiftc -warnings-as-errors` passes
3. `swift test --filter LensSystemTests` passes with 12/12 tests
4. No `ConfigBuilder<T>` return types remain in the file (all replaced with `Self`)
5. No `private init(config: T)` exists in the file
</verification>

<success_criteria>
- Zero SwiftLint violations in ConfigBuilder.swift
- Build passes with warnings-as-errors
- LensSystemTests still pass (ConfigBuilder functionality preserved)
- Phase 04.7 verification will now show 9/9 must-haves verified
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-26-SUMMARY.md`
</output>
