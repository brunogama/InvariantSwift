---
phase: 04.7-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/InvariantSwift/Core/FailureReport.swift
  - Sources/InvariantSwift/Core/AnySendable.swift
  - Sources/InvariantSwift/Macros/PureMacroDeclaration.swift
  - Sources/InvariantSwift/Ghostwriter/SourceAnalyzer.swift
  - Sources/InvariantSwift/Presentation/PrettyPrint.swift
  - Sources/InvariantSwift/SwiftTesting/PropertyTestIntegration.swift
  - Sources/InvariantSwift/Advanced/GeneratorMiddleware.swift
  - Sources/InvariantSwift/Database/CorpusDatabase.swift
autonomous: true

must_haves:
  truths:
    - "No print statements in production Sources code without swiftlint disable"
    - "All superfluous disable commands removed from Sources"
    - "All blanket disable commands converted to targeted disables in Sources"
    - "All line length violations fixed in Sources"
  artifacts:
    - path: "Sources/InvariantSwift/Core/FailureReport.swift"
      provides: "Clean FailureReport without print violations"
    - path: "Sources/InvariantSwift/Core/AnySendable.swift"
      provides: "Clean AnySendable without blanket disables"
    - path: "Sources/InvariantSwift/Macros/PureMacroDeclaration.swift"
      provides: "Clean PureMacroDeclaration without print violations"
  key_links:
    - from: "swiftlint lint --strict"
      to: "Sources/"
      via: "no print violations in Sources"
      pattern: "no_print.*Sources"
---

<objective>
Fix SwiftLint violations in InvariantSwift Sources (non-macro implementation files)

Purpose: Clean up print statements, line length violations, and disable command issues in the main library Sources. This establishes a clean foundation before tackling Tests.
Output: Zero SwiftLint violations in Sources/InvariantSwift/ (excluding InvariantSwiftMacros/)
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-codebase-cleanup/04.7-RESEARCH.md
@RULES.md
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix print statement violations in Sources</name>
  <files>
    Sources/InvariantSwift/Core/FailureReport.swift
    Sources/InvariantSwift/Macros/PureMacroDeclaration.swift
  </files>
  <action>
Fix the 14 `no_print` violations in Sources files:

1. FailureReport.swift (line 23): Remove or wrap debug print statement
   - If print is for debugging, wrap in `#if DEBUG` block
   - If print is for user output in CLI context, use OSLog or Logger
   - If print can be removed entirely, remove it

2. PureMacroDeclaration.swift (lines 21, 96): Remove debug prints from macro declaration
   - These are likely debug statements from development
   - Remove entirely or wrap in `#if DEBUG` if needed for development debugging

Pattern to apply:
```swift
// REMOVE this:
print("debug message")

// OR wrap in DEBUG if needed:
#if DEBUG
print("debug message")
#endif
```

Do NOT use swiftlint:disable. Fix the actual code.
  </action>
  <verify>
Run: `swiftlint lint --strict Sources/InvariantSwift/Core/FailureReport.swift Sources/InvariantSwift/Macros/PureMacroDeclaration.swift 2>&1 | grep no_print`
Expected: No output (no print violations)
  </verify>
  <done>FailureReport.swift and PureMacroDeclaration.swift have zero no_print violations</done>
</task>

<task type="auto">
  <name>Task 2: Fix line length violations and disable commands in Sources</name>
  <files>
    Sources/InvariantSwift/Ghostwriter/SourceAnalyzer.swift
    Sources/InvariantSwift/Core/AnySendable.swift
    Sources/InvariantSwift/Presentation/PrettyPrint.swift
    Sources/InvariantSwift/SwiftTesting/PropertyTestIntegration.swift
  </files>
  <action>
Fix line length and disable command violations:

1. SourceAnalyzer.swift (lines 89-90):
   - Line 89: superfluous disable command - remove it
   - Line 90: line length 137 chars - break into multiple lines
   - Long strings should be split: `"long" + "string"`
   - Long chains should break at dots: `.method1()\n.method2()`

2. AnySendable.swift (8 violations):
   - Convert blanket disables to targeted `:next` or `:this` disables
   - Or refactor code to not need disables at all
   - Blanket pattern: `// swiftlint:disable rule` -> `// swiftlint:disable:next rule`

3. PrettyPrint.swift:
   - Remove any superfluous disable commands
   - Fix line length violations by breaking long lines

4. PropertyTestIntegration.swift:
   - Remove superfluous disables
   - Convert blanket to targeted disables

Pattern for line length:
```swift
// BEFORE (over 100 chars)
let result = someVeryLongFunctionName(parameter1: value1, parameter2: value2, parameter3: value3)

// AFTER (each under 100 chars)
let result = someVeryLongFunctionName(
  parameter1: value1,
  parameter2: value2,
  parameter3: value3
)
```

Pattern for blanket -> targeted:
```swift
// BEFORE (blanket)
// swiftlint:disable type_body_length
struct LargeStruct { ... }

// AFTER (targeted)
// swiftlint:disable:next type_body_length
struct LargeStruct { ... }
```
  </action>
  <verify>
Run: `swiftlint lint --strict Sources/InvariantSwift/Ghostwriter/ Sources/InvariantSwift/Core/AnySendable.swift Sources/InvariantSwift/Presentation/PrettyPrint.swift Sources/InvariantSwift/SwiftTesting/PropertyTestIntegration.swift 2>&1 | grep -E "line_length|superfluous|blanket"`
Expected: No output
  </verify>
  <done>SourceAnalyzer, AnySendable, PrettyPrint, PropertyTestIntegration have zero line_length, superfluous, or blanket violations</done>
</task>

<task type="auto">
  <name>Task 3: Fix remaining Sources violations and verify clean build</name>
  <files>
    Sources/InvariantSwift/Advanced/GeneratorMiddleware.swift
    Sources/InvariantSwift/Database/CorpusDatabase.swift
    Sources/InvariantSwift/Coverage/ClassificationCoverage.swift
    Sources/InvariantSwift/SwiftTesting/TestStatistics.swift
  </files>
  <action>
Fix any remaining SwiftLint violations in Sources/InvariantSwift/:

1. Run `swiftlint lint --strict Sources/InvariantSwift/ 2>&1 | grep error:` to find remaining issues
2. For each violation:
   - Remove superfluous disables
   - Convert blankets to targeted disables (or refactor)
   - Fix line length by breaking long lines
   - Fix closure_parameter_position by moving params to same line as brace

3. After fixes, verify with:
   - `swift build -Xswiftc -warnings-as-errors`
   - `swiftlint lint --strict Sources/InvariantSwift/`

Target: Zero violations in Sources/InvariantSwift/ (macro implementations in InvariantSwiftMacros/ handled in Plan 02)
  </action>
  <verify>
Run: `swiftlint lint --strict Sources/InvariantSwift/ 2>&1 | grep "error:" | wc -l`
Expected: 0

Run: `swift build -Xswiftc -warnings-as-errors 2>&1 | grep -i "warning\|error" | head -5`
Expected: Build succeeds with no warnings/errors
  </verify>
  <done>Sources/InvariantSwift/ has zero SwiftLint violations and builds with warnings-as-errors</done>
</task>

</tasks>

<verification>
Overall verification for Plan 01:

1. SwiftLint check: `swiftlint lint --strict Sources/InvariantSwift/ 2>&1 | grep "error:" | wc -l` = 0
2. Build check: `swift build -Xswiftc -warnings-as-errors` succeeds
3. No blanket disables: `rg "swiftlint:disable [a-z]" --type swift Sources/InvariantSwift/ | grep -v ":next\|:this\|:previous" | wc -l` = 0
</verification>

<success_criteria>
- Sources/InvariantSwift/ has zero SwiftLint violations in strict mode
- No blanket disable commands remain (all converted to targeted)
- Build passes with -Xswiftc -warnings-as-errors
- No print statements in production code (or properly wrapped in #if DEBUG)
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-01-SUMMARY.md`
</output>
