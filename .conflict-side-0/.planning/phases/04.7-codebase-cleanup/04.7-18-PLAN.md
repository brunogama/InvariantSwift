---
phase: 04.7-codebase-cleanup
plan: 18
type: execute
wave: 6
depends_on: ["04.7-14", "04.7-15", "04.7-16", "04.7-17"]
gap_closure: true
files_modified:
  - Tests/InvariantSwiftTests/FunctionalTesting/FailurePersistenceTests.swift
  - Tests/InvariantSwiftTests/FunctionalTesting/LinearizabilityTests.swift
  - Tests/InvariantSwiftTests/FunctionalTesting/LensSystemTests.swift
autonomous: true

must_haves:
  truths:
    - "FailurePersistenceTests.swift compiles with zero errors"
    - "LinearizabilityTests.swift compiles with zero errors"
    - "LensSystemTests.swift compiles with zero errors"
    - "All 3 files have zero SwiftLint violations"
    - "No .swift.disabled files remain in Tests/"
  artifacts:
    - path: "Tests/InvariantSwiftTests/FunctionalTesting/FailurePersistenceTests.swift"
      provides: "Re-enabled failure persistence tests"
    - path: "Tests/InvariantSwiftTests/FunctionalTesting/LinearizabilityTests.swift"
      provides: "Re-enabled linearizability tests"
    - path: "Tests/InvariantSwiftTests/FunctionalTesting/LensSystemTests.swift"
      provides: "Re-enabled lens system tests"
  key_links:
    - from: "All infrastructure"
      to: "Final 3 test files"
      via: "complete API availability"
---

<objective>
Re-enable final 3 test files after infrastructure completion (Final Gap Closure)

Purpose: With all infrastructure in place (lenses, PrettyPrinter, LibFuzzer, Coverage), re-enable and fix the final 3 test files.
Output: Zero .swift.disabled files remain, all tests compile
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-codebase-cleanup/04.7-13-SUMMARY.md
@.planning/phases/04.7-codebase-cleanup/04.7-14-SUMMARY.md
@.planning/phases/04.7-codebase-cleanup/04.7-15-SUMMARY.md
@.planning/phases/04.7-codebase-cleanup/04.7-16-SUMMARY.md
@.planning/phases/04.7-codebase-cleanup/04.7-17-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FailurePersistenceTests with lens/config infrastructure</name>
  <files>
    Tests/InvariantSwiftTests/FunctionalTesting/FailurePersistenceTests.swift
  </files>
  <action>
Fix FailurePersistenceTests with newly available infrastructure:

1. Build and capture current errors:
   ```bash
   swift build --target InvariantSwiftTests 2>&1 | grep "FailurePersistenceTests" -A 3 | head -50
   ```

2. Fix Size/Seed lens usage:
   - Replace `Size.value` calls with lens API
   - Replace `Seed.seedValue` calls with lens API
   - Update `Size.increment` calls

3. Fix PropertyConfig helper usage:
   - Replace config construction with `.quickConfig()` or `.performanceConfig()`

4. Fix actor-based ExampleDatabase calls:
   - Add `await` to async method calls
   - Update method signatures if needed

5. Verify zero compilation errors:
   ```bash
   swift build --target InvariantSwiftTests 2>&1 | grep "FailurePersistenceTests.*error:" | wc -l
   ```

6. Run SwiftLint:
   ```bash
   swiftlint lint --strict Tests/InvariantSwiftTests/FunctionalTesting/FailurePersistenceTests.swift
   ```
  </action>
  <verify>
Run: `swift build --target InvariantSwiftTests 2>&1 | grep -c "error.*FailurePersistenceTests"`
Expected: 0

Run: `swiftlint lint --strict Tests/InvariantSwiftTests/FunctionalTesting/FailurePersistenceTests.swift 2>&1 | grep -c "error:"`
Expected: 0
  </verify>
  <done>FailurePersistenceTests compiles with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Fix LinearizabilityTests with PrettyPrinter infrastructure</name>
  <files>
    Tests/InvariantSwiftTests/FunctionalTesting/LinearizabilityTests.swift
  </files>
  <action>
Fix LinearizabilityTests with PrettyPrinter infrastructure:

1. Build and capture current errors:
   ```bash
   swift build --target InvariantSwiftTests 2>&1 | grep "LinearizabilityTests" -A 3 | head -50
   ```

2. Fix PrettyPrinter usage:
   - Import PrettyPrinter types
   - Replace `PrettyPrinter.print()` calls with new API
   - Update `PrettyPrintable` conformances

3. Fix DiffFormat usage:
   - Update diff rendering calls
   - Use `DiffResult.render()` method

4. Fix generic Operation usage:
   - Update `Operation<Int, Int>` generic syntax
   - Fix any type parameter issues

5. Verify zero compilation errors:
   ```bash
   swift build --target InvariantSwiftTests 2>&1 | grep "LinearizabilityTests.*error:" | wc -l
   ```

6. Run SwiftLint:
   ```bash
   swiftlint lint --strict Tests/InvariantSwiftTests/FunctionalTesting/LinearizabilityTests.swift
   ```
  </action>
  <verify>
Run: `swift build --target InvariantSwiftTests 2>&1 | grep -c "error.*LinearizabilityTests"`
Expected: 0

Run: `swiftlint lint --strict Tests/InvariantSwiftTests/FunctionalTesting/LinearizabilityTests.swift 2>&1 | grep -c "error:"`
Expected: 0
  </verify>
  <done>LinearizabilityTests compiles with zero errors</done>
</task>

<task type="auto">
  <name>Task 3: Fix LensSystemTests with all infrastructure</name>
  <files>
    Tests/InvariantSwiftTests/FunctionalTesting/LensSystemTests.swift
  </files>
  <action>
Fix LensSystemTests with complete infrastructure:

1. Build and capture current errors:
   ```bash
   swift build --target InvariantSwiftTests 2>&1 | grep "LensSystemTests" -A 3 | head -50
   ```

2. Fix PropertyConfig lens usage:
   - Use `PropertyConfig.iterations` lens
   - Use `PropertyConfig.maxShrinks` lens
   - Use `PropertyConfig.seed` lens

3. Fix Size/Seed lens usage:
   - Apply lens composition if needed

4. Fix Coverage type imports:
   - Import from InvariantSwiftExperimental
   - Use public Coverage types

5. Fix any remaining LibFuzzer references:
   - Use conditional compilation or skip conditions

6. Verify zero compilation errors:
   ```bash
   swift build --target InvariantSwiftTests 2>&1 | grep "LensSystemTests.*error:" | wc -l
   ```

7. Run SwiftLint:
   ```bash
   swiftlint lint --strict Tests/InvariantSwiftTests/FunctionalTesting/LensSystemTests.swift
   ```
  </action>
  <verify>
Run: `swift build --target InvariantSwiftTests 2>&1 | grep -c "error.*LensSystemTests"`
Expected: 0

Run: `swiftlint lint --strict Tests/InvariantSwiftTests/FunctionalTesting/LensSystemTests.swift 2>&1 | grep -c "error:"`
Expected: 0
  </verify>
  <done>LensSystemTests compiles with zero errors</done>
</task>

<task type="auto">
  <name>Task 4: Verify no disabled files remain</name>
  <files>
    Tests/
  </files>
  <action>
Final verification that all test files are re-enabled:

1. Check for any remaining .swift.disabled files:
   ```bash
   find Tests -name "*.swift.disabled" -type f -not -path "./.build/*"
   ```

2. If any found, investigate and re-enable:
   - Determine why still disabled
   - Fix any remaining issues
   - Rename to .swift

3. Verify full test suite compiles:
   ```bash
   swift build --build-tests 2>&1 | tail -10
   ```

4. Run comprehensive SwiftLint check:
   ```bash
   swiftlint lint --strict Tests/ 2>&1 | grep "error:" | wc -l
   ```

5. Update DEFERRED.md:
   - Mark all files as re-enabled
   - Document any skip conditions added
  </action>
  <verify>
Run: `find Tests -name "*.swift.disabled" -type f | wc -l`
Expected: 0 (zero disabled files)

Run: `swift build --build-tests 2>&1 | tail -3`
Expected: "Build complete!"

Run: `swiftlint lint --strict Tests/ 2>&1 | grep -c "error:"`
Expected: 0
  </verify>
  <done>All test files re-enabled, zero .swift.disabled files remain</done>
</task>

</tasks>

<verification>
Overall verification for Plan 18:

1. No disabled files: `find Tests -name "*.swift.disabled" | wc -l` = 0
2. All tests compile: `swift build --build-tests 2>&1 | grep -c "error:"` = 0
3. SwiftLint clean: `swiftlint lint --strict Tests/ 2>&1 | grep -c "error:"` = 0
4. Final 3 files enabled: `ls Tests/InvariantSwiftTests/FunctionalTesting/{FailurePersistence,Linearizability,LensSystem}Tests.swift | wc -l` = 3
</verification>

<success_criteria>
- FailurePersistenceTests.swift compiles with zero errors
- LinearizabilityTests.swift compiles with zero errors
- LensSystemTests.swift compiles with zero errors
- Zero .swift.disabled files in Tests/ directory
- All re-enabled files pass SwiftLint strict mode
- Full test suite builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-18-SUMMARY.md`
</output>
