---
phase: 04.7-codebase-cleanup
plan: 07
type: execute
wave: 3
depends_on: ["04.7-03", "04.7-04", "04.7-05", "04.7-10"]
files_modified:
  - Tests/InvariantSwiftTests/FunctionalTesting/SMTSolverTests.swift.disabled
  - Tests/InvariantSwiftTests/FunctionalTesting/LinearizabilityTests.swift.disabled
  - Tests/InvariantSwiftTests/FunctionalTesting/LensSystemTests.swift.disabled
  - Tests/InvariantSwiftTests/FunctionalTesting/MetamorphicTests.swift.disabled
  - Tests/InvariantSwiftTests/FunctionalTesting/GeneratorRegistryTests.swift.disabled
  - Tests/InvariantSwiftTests/FunctionalTesting/CoverageGuidedTests.swift.disabled
  - Tests/InvariantSwiftTests/FunctionalTesting/CoverageCompletionTests.swift.disabled
  - Sources/InvariantSwift/Macros/LawGeneration.swift.disabled
autonomous: true

must_haves:
  truths:
    - "All 16 disabled test files addressed (re-enabled or documented as deferred)"
    - "Experimental feature tests compile if dependencies available"
    - "LawGeneration.swift.disabled in Sources addressed"
    - "No .swift.disabled files remain without documented reason"
  artifacts:
    - path: "Tests/InvariantSwiftTests/FunctionalTesting/"
      provides: "All test files enabled or documented"
  key_links:
    - from: ".swift.disabled files"
      to: "enabled or documented"
      via: "re-enable or add DEFERRED.md note"
---

<objective>
Re-enable remaining disabled test files and LawGeneration source (batch 2: experimental features)

Purpose: Address the remaining 8 disabled files. These test experimental features (SMT, Linearizability, Coverage-guided) that may have significant API changes or missing dependencies. Some may need to be deferred if the underlying feature is incomplete.
Output: All .swift.disabled files either re-enabled or documented as intentionally deferred
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-codebase-cleanup/04.7-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Address SMT, Linearizability, and Lens test files</name>
  <files>
    Tests/InvariantSwiftTests/FunctionalTesting/SMTSolverTests.swift.disabled
    Tests/InvariantSwiftTests/FunctionalTesting/LinearizabilityTests.swift.disabled
    Tests/InvariantSwiftTests/FunctionalTesting/LensSystemTests.swift.disabled
  </files>
  <action>
Address experimental feature test files using systematic API discovery:

**For each file:**

1. **Check if feature exists in current codebase:**
   ```bash
   rg "SMTSolver|SMT" Sources/ | head -10
   rg "Linearizability|linearizable" Sources/ | head -10
   rg "Lens|Prism|Optic" Sources/ | head -10
   ```

2. **If feature exists:**
   a) Document old->new API mapping
   b) Rename .swift.disabled to .swift
   c) Build and capture errors: `swift build --target InvariantSwiftTests 2>&1 | grep error:`
   d) Fix API calls based on mapping
   e) Verify zero errors

3. **If feature incomplete/missing:**
   a) Document in DEFERRED.md with reason
   b) Keep file disabled
   c) Add comment in disabled file explaining deferral

**SMTSolverTests.swift.disabled:**
```bash
rg "SMTSolver|SMT" Sources/
```
- If found: Re-enable and update APIs
- If not: Document "SMT solver integration - feature not yet implemented"

**LinearizabilityTests.swift.disabled:**
```bash
rg "Linearizability|linearizable" Sources/
```
- Linearizability testing often requires InvariantSwiftExperimental
- Check: `rg "target.*Experimental" Package.swift`
- If experimental module exists: Re-enable with import
- If not: Document as deferred

**LensSystemTests.swift.disabled:**
```bash
rg "Lens|Prism|Optic" Sources/
```
- Lens/Prism are functional optics patterns
- If found: Re-enable and update APIs
- If not: Document as deferred
  </action>
  <verify>
For each file, document status:
- File is .swift (re-enabled) AND `swift build --target InvariantSwiftTests 2>&1 | grep error:` = 0
- OR file is .swift.disabled AND documented in DEFERRED.md

Run: `ls Tests/InvariantSwiftTests/FunctionalTesting/{SMT,Linearizability,LensSystem}*Tests.swift* 2>/dev/null`
Document whether each is .swift or .swift.disabled with reason
  </verify>
  <done>SMT, Linearizability, and Lens test files addressed (re-enabled with zero errors or documented in DEFERRED.md)</done>
</task>

<task type="auto">
  <name>Task 2: Address Metamorphic, Registry, and Coverage test files</name>
  <files>
    Tests/InvariantSwiftTests/FunctionalTesting/MetamorphicTests.swift.disabled
    Tests/InvariantSwiftTests/FunctionalTesting/GeneratorRegistryTests.swift.disabled
    Tests/InvariantSwiftTests/FunctionalTesting/CoverageGuidedTests.swift.disabled
    Tests/InvariantSwiftTests/FunctionalTesting/CoverageCompletionTests.swift.disabled
  </files>
  <action>
Address remaining experimental test files:

**MetamorphicTests.swift.disabled:**
```bash
rg "Metamorphic" Sources/ | head -10
```
- Document API mapping if found
- Re-enable and fix, or document as deferred

**GeneratorRegistryTests.swift.disabled:**
```bash
rg "GeneratorRegistry" Sources/ | head -10
rg "registry|Registry" Sources/InvariantSwift/Generators/ | head -10
```
- Registry may have been replaced with different pattern
- Document replacement pattern if applicable

**CoverageGuidedTests.swift.disabled:**
```bash
rg "CoverageGuided|coverage" Sources/ | head -10
```
- Coverage-guided testing depends on profraw infrastructure
- Re-enable with skip condition for missing profraw:
  ```swift
  @Test func testCoverageGuided() throws {
    try skipUnlessCoverageAvailable()
    // test body
  }

  private func skipUnlessCoverageAvailable() throws {
    let profrawPath = FileManager.default.currentDirectoryPath + "/.build/debug/codecov"
    guard FileManager.default.fileExists(atPath: profrawPath) else {
      throw XCTSkip("Coverage data not available - run with --enable-code-coverage")
    }
  }
  ```

**CoverageCompletionTests.swift.disabled:**
- Similar to CoverageGuidedTests
- Re-enable with appropriate skip conditions

**Important:** Coverage tests should be ENABLED with skip conditions, NOT left disabled. This ensures they run when coverage is available.
  </action>
  <verify>
For coverage tests specifically:
Run: `ls Tests/InvariantSwiftTests/FunctionalTesting/{CoverageGuided,CoverageCompletion}*Tests.swift 2>/dev/null | wc -l`
Expected: 2 (both enabled, with skip conditions)

Run: `swift build --target InvariantSwiftTests 2>&1 | grep -c "error:"`
Expected: 0

For all files:
Document status (enabled or deferred with reason)
  </verify>
  <done>Metamorphic, Registry, and Coverage test files addressed (re-enabled or documented)</done>
</task>

<task type="auto">
  <name>Task 3: Address LawGeneration.swift.disabled and create DEFERRED.md</name>
  <files>
    Sources/InvariantSwift/Macros/LawGeneration.swift.disabled
  </files>
  <action>
Address the source file and document all deferred items:

**LawGeneration.swift.disabled:**

1. This is in Sources/, not Tests/ - more significant
   ```bash
   rg "LawGeneration" Sources/ | head -10
   ```

2. Check if related to @LawChecked macro (exposed in Phase 4.2):
   ```bash
   rg "@LawChecked|LawCheckedMacro" Sources/ | head -10
   ```

3. Determine action:
   - If needed by current macro: Re-enable with API updates
   - If superseded by current implementation: Delete entirely
   - If future feature: Keep disabled with documentation

4. Verify no orphaned references if deleted:
   ```bash
   rg "LawGeneration" Sources/ Tests/
   ```

**Create/update DEFERRED.md:**

If ANY files remain disabled, create `.planning/phases/04.7-codebase-cleanup/DEFERRED.md`:

```markdown
# Deferred Test Files - Phase 4.7

The following test files remain disabled pending feature completion or infrastructure availability:

## [FileName].swift.disabled

- **Feature:** [Feature name]
- **Status:** [Incomplete | Missing dependency | Infrastructure required]
- **Reason:** [Specific reason for deferral]
- **Unblock:** [What needs to happen to re-enable]

## Example entries:

## SMTSolverTests.swift.disabled
- **Feature:** SMT solver integration
- **Status:** Feature incomplete
- **Reason:** SMTSolver type not implemented in Sources/
- **Unblock:** Implement SMTSolver in Sources/InvariantSwift/Advanced/

## LinearizabilityTests.swift.disabled
- **Feature:** Linearizability testing for concurrent data structures
- **Status:** Experimental module incomplete
- **Reason:** InvariantSwiftExperimental target not fully implemented
- **Unblock:** Complete experimental module and expose Linearizability API
```

**Final inventory:**
```bash
find . -name "*.swift.disabled" -type f -not -path "./.build/*"
```
- All files must be either:
  1. Re-enabled (no .disabled extension)
  2. Documented in DEFERRED.md with clear reason and unblock criteria
  </action>
  <verify>
Run: `find . -name "*.swift.disabled" -type f -not -path "./.build/*" | wc -l`
Document remaining count

If count > 0:
Run: `cat .planning/phases/04.7-codebase-cleanup/DEFERRED.md | head -50`
Verify all remaining disabled files are documented

Run: `swift build --target InvariantSwiftTests 2>&1 | grep -c "error:"`
Expected: 0
  </verify>
  <done>All .swift.disabled files either re-enabled or documented in DEFERRED.md with reasons and unblock criteria</done>
</task>

</tasks>

<verification>
Overall verification for Plan 07:

1. Disabled file inventory: `find . -name "*.swift.disabled" -type f -not -path "./.build/*"`
   - Either 0 files (all re-enabled)
   - OR all remaining files documented in DEFERRED.md
2. Build: `swift build --build-tests 2>&1 | grep -c "error:"` = 0
3. Documentation: If any files remain disabled, DEFERRED.md exists with complete documentation
4. Coverage tests: Enabled with skip conditions, not left disabled
</verification>

<success_criteria>
- All 16 disabled test files addressed
- LawGeneration.swift.disabled addressed (re-enabled, deleted, or documented)
- Re-enabled files compile successfully (verified: grep -c "error:" = 0)
- Coverage tests have skip conditions for missing infrastructure (not left disabled)
- DEFERRED.md documents any files that cannot be re-enabled with:
  - Clear reason
  - Unblock criteria (what needs to happen)
- Zero unexplained .swift.disabled files remain
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-07-SUMMARY.md`
</output>
