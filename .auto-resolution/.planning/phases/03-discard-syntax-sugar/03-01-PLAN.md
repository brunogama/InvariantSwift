---
phase: 03-discard-syntax-sugar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/InvariantSwift/Core/Property+Implication.swift
  - Sources/InvariantSwift/FunctionalTesting.swift
autonomous: true

must_haves:
  truths:
    - "Developer can use `precondition ==> consequent` syntax in property tests"
    - "False precondition returns .discard (test case skipped, not failed)"
    - "True precondition evaluates consequent and returns .pass or .fail"
    - "Consequent is short-circuit evaluated (not evaluated when precondition false)"
  artifacts:
    - path: "Sources/InvariantSwift/Core/Property+Implication.swift"
      provides: "Implication operator ==> with correct precedence"
      exports: ["==>"]
      min_lines: 40
  key_links:
    - from: "Sources/InvariantSwift/Core/Property+Implication.swift"
      to: "PropertyEvaluation"
      via: "Returns .pass, .fail, or .discard"
      pattern: "return.*\\.discard|return.*\\.pass|return.*\\.fail"
    - from: "Sources/InvariantSwift/FunctionalTesting.swift"
      to: "Property+Implication.swift"
      via: "Re-export for public API"
      pattern: "@_exported import|public.*==>"
---

<objective>
Implement the `==>` implication operator for QuickCheck-style conditional properties.

Purpose: Enable readable precondition syntax that automatically discards test cases when preconditions are not met, matching QuickCheck's `==>` semantics exactly.

Output: `Property+Implication.swift` with `==>` operator exported via `FunctionalTesting.swift`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-discard-syntax-sugar/03-RESEARCH.md

@Sources/InvariantSwift/Core/Property.swift (lines 60-122 for PropertyEvaluation, assume, require)
@Sources/InvariantSwift/FunctionalTesting.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Property+Implication.swift with ==> operator</name>
  <files>Sources/InvariantSwift/Core/Property+Implication.swift</files>
  <action>
Create a new file `Property+Implication.swift` implementing the `==>` implication operator:

1. Define precedence group `ImplicationPrecedence`:
   - `higherThan: AssignmentPrecedence`
   - `lowerThan: ComparisonPrecedence`
   - `associativity: right` (a ==> b ==> c means a ==> (b ==> c))

2. Define `infix operator ==> : ImplicationPrecedence`

3. Implement two overloads:

   **Overload 1: Bool consequent (common case)**
   ```swift
   public func ==> (
     precondition: Bool,
     consequent: @autoclosure () -> Bool
   ) -> PropertyEvaluation {
     guard precondition else { return .discard(reason: nil) }
     return consequent() ? .pass : .fail(reason: nil)
   }
   ```

   **Overload 2: PropertyEvaluation consequent (explicit control)**
   ```swift
   public func ==> (
     precondition: Bool,
     consequent: @autoclosure () -> PropertyEvaluation
   ) -> PropertyEvaluation {
     guard precondition else { return .discard(reason: nil) }
     return consequent()
   }
   ```

4. Add comprehensive documentation:
   - Doc comments explaining QuickCheck semantics
   - Examples showing usage with @PropertyTest
   - Note about short-circuit evaluation
   - Comparison with `assume()` function

Follow existing code style in Property.swift (doc comments, spacing, MARK comments).
  </action>
  <verify>
`swift build -Xswiftc -warnings-as-errors` compiles without errors or warnings.
`swiftlint lint --strict Sources/InvariantSwift/Core/Property+Implication.swift` passes.
  </verify>
  <done>
File exists at `Sources/InvariantSwift/Core/Property+Implication.swift` with:
- ImplicationPrecedence precedence group defined
- `==>` operator with two overloads (Bool and PropertyEvaluation consequent)
- @autoclosure for short-circuit evaluation
- Documentation matching existing Property.swift style
  </done>
</task>

<task type="auto">
  <name>Task 2: Export ==> operator via FunctionalTesting.swift</name>
  <files>Sources/InvariantSwift/FunctionalTesting.swift</files>
  <action>
Add the `==>` operator to the public API exports in `FunctionalTesting.swift`.

Since the operator is defined at module scope in Property+Implication.swift, it will be automatically available when importing InvariantSwift. However, verify:

1. The file `Property+Implication.swift` is in the InvariantSwift target (no Package.swift changes needed - it's in Sources/InvariantSwift/Core/)

2. If FunctionalTesting.swift uses explicit re-exports, add a comment noting the ==> operator is available.

3. Verify the operator is accessible by checking compilation of a simple test expression.

No code changes may be needed if the module automatically exports all public symbols. Document this verification.
  </action>
  <verify>
Create a small verification script or use REPL to confirm:
```swift
import InvariantSwift
let result: PropertyEvaluation = true ==> false
```
compiles and runs.
  </verify>
  <done>
The `==>` operator is accessible when importing InvariantSwift. Verification confirms usage works as expected.
  </done>
</task>

</tasks>

<verification>
1. `swift build -Xswiftc -warnings-as-errors` passes
2. `swiftlint lint --strict Sources/InvariantSwift/Core/Property+Implication.swift` passes
3. Operator has correct precedence: `n > 0 ==> property` parses as `(n > 0) ==> property`
4. Short-circuit evaluation: consequent not evaluated when precondition is false
5. Both overloads work: Bool consequent and PropertyEvaluation consequent
</verification>

<success_criteria>
- `==>` operator defined with ImplicationPrecedence (higherThan: Assignment, lowerThan: Comparison, right associative)
- Two overloads: Bool -> PropertyEvaluation and PropertyEvaluation -> PropertyEvaluation
- @autoclosure ensures short-circuit evaluation
- Documentation complete with examples
- Zero warnings, zero lint violations
</success_criteria>

<output>
After completion, create `.planning/phases/03-discard-syntax-sugar/03-01-SUMMARY.md`
</output>
