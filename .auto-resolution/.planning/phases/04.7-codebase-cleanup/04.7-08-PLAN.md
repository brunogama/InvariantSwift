---
phase: 04.7-codebase-cleanup
plan: 08
type: execute
wave: 4
depends_on: ["04.7-06", "04.7-07"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "`swiftlint lint --strict` passes with zero violations"
    - "`swift build -Xswiftc -warnings-as-errors` passes"
    - "`swift test` passes with zero failures (tests may skip)"
    - "All enabled tests compile successfully"
    - "Phase 4.7 cleanup complete"
  artifacts:
    - path: ".planning/phases/04.7-codebase-cleanup/"
      provides: "Complete phase documentation"
  key_links:
    - from: "swift test"
      to: "passing tests"
      via: "all tests pass or skip gracefully"
---

<objective>
Final verification checkpoint for Phase 4.7 codebase cleanup

Purpose: Verify all cleanup work is complete before marking phase as done. This is a human-verify checkpoint to ensure quality gates pass.
Output: Confirmation that all quality gates pass or documented exceptions
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run comprehensive SwiftLint verification</name>
  <files></files>
  <action>
Run full SwiftLint verification across the codebase:

1. Sources verification:
   ```bash
   swiftlint lint --strict Sources/ 2>&1 | grep error:
   ```
   Expected: No output (zero violations)

2. Tests verification:
   ```bash
   swiftlint lint --strict Tests/ 2>&1 | grep error:
   ```
   Expected: No output (zero violations)

3. Total violation count:
   ```bash
   swiftlint lint --strict 2>&1 | grep "Found .* violations"
   ```
   Expected: "Found 0 violations, 0 serious"

4. If violations found:
   - List each violation
   - Determine if it's a regression from earlier plans
   - Fix immediately or document why it cannot be fixed
  </action>
  <verify>
Run: `swiftlint lint --strict 2>&1 | tail -5`
Expected: "Done linting! Found 0 violations, 0 serious"
  </verify>
  <done>SwiftLint passes with zero violations</done>
</task>

<task type="auto">
  <name>Task 2: Run warnings-as-errors build and test verification</name>
  <files></files>
  <action>
Verify the codebase builds and tests cleanly:

1. Clean build cache:
   ```bash
   swift package clean
   ```

2. Build with strict warnings:
   ```bash
   swift build -Xswiftc -warnings-as-errors 2>&1 | tee /tmp/build_output.txt
   ```

3. Check for warnings/errors:
   ```bash
   grep -E "warning:|error:" /tmp/build_output.txt | head -20
   ```
   Expected: Only SPM "unhandled files" warnings (acceptable) or zero output

4. Verify build success:
   ```bash
   tail -5 /tmp/build_output.txt
   ```
   Expected: "Build complete!"

5. Build tests:
   ```bash
   swift build --build-tests -Xswiftc -warnings-as-errors 2>&1 | tail -10
   ```
   Expected: "Build complete!"

6. Run tests (may have some skips, but no failures):
   ```bash
   swift test 2>&1 | tee /tmp/test_output.txt
   tail -20 /tmp/test_output.txt
   ```
   Expected: All tests pass or skip (no failures)

7. Check for failures:
   ```bash
   grep -E "failed|FAILED" /tmp/test_output.txt | wc -l
   ```
   Expected: 0
  </action>
  <verify>
Run: `swift build -Xswiftc -warnings-as-errors 2>&1 | tail -3`
Expected: Contains "Build complete!"

Run: `swift test 2>&1 | grep -E "Test.*failed" | wc -l`
Expected: 0 (no test failures)
  </verify>
  <done>Build passes with warnings-as-errors and tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete codebase cleanup for Phase 4.7 including:
- Fixed compiler warnings (Plan 09)
- Fixed all SwiftLint violations across Sources and Tests (Plans 01-05)
- Fixed 13 failing enabled tests (Plan 10)
- Re-enabled 16 disabled test files (Plans 06-07) or documented deferrals
- All quality gates passing
  </what-built>
  <how-to-verify>
1. Verify SwiftLint passes:
   ```bash
   swiftlint lint --strict 2>&1 | tail -3
   ```
   Expected: "Found 0 violations, 0 serious"

2. Verify build passes:
   ```bash
   swift build -Xswiftc -warnings-as-errors 2>&1 | tail -3
   ```
   Expected: "Build complete!"

3. Verify tests pass:
   ```bash
   swift test 2>&1 | tail -10
   ```
   Expected: No failures (skips are OK)

4. Check for remaining disabled files:
   ```bash
   find . -name "*.swift.disabled" -type f -not -path "./.build/*"
   ```
   Expected: 0 files OR all documented in DEFERRED.md

5. If any disabled files remain:
   ```bash
   cat .planning/phases/04.7-codebase-cleanup/DEFERRED.md
   ```
   Verify each file has documented reason and unblock criteria

6. Final quality gate summary:
   - [ ] `swiftlint lint --strict` = 0 violations
   - [ ] `swift build -Xswiftc -warnings-as-errors` succeeds
   - [ ] `swift test` = 0 failures
   - [ ] All .swift.disabled files addressed
  </how-to-verify>
  <resume-signal>Type "approved" if all quality gates pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Final Phase 4.7 verification checklist:

1. [ ] `swiftlint lint --strict` = 0 violations
2. [ ] `swift build -Xswiftc -warnings-as-errors` succeeds
3. [ ] `swift build --build-tests` succeeds
4. [ ] `swift test` = 0 failures (skips OK)
5. [ ] All .swift.disabled files addressed (re-enabled or documented)
6. [ ] No blanket disable commands remain
7. [ ] RULES.md budgets respected (or justified exceptions documented)
</verification>

<success_criteria>
- Zero SwiftLint violations in strict mode
- Build passes with warnings-as-errors
- All tests pass (skips are acceptable)
- All 16 disabled test files addressed
- Human verification confirms quality gates pass
- Phase 4.7 ready to mark as complete
</success_criteria>

<output>
After approval, create `.planning/phases/04.7-codebase-cleanup/04.7-08-SUMMARY.md` and update STATE.md
</output>
