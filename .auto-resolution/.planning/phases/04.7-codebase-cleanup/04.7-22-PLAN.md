---
phase: 04.7-codebase-cleanup
plan: 22
type: execute
wave: 2
depends_on: [19, 20]
files_modified:
  - Tests/InvariantSwiftTests/TestHelpers/DiffAssertions.swift
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "expectNoDifference function exists and is accessible from test code"
    - "expectDifference function exists and is accessible from test code"
    - "PrettyPrinterEnhancementTests.swift compiles successfully"
    - "swift build --build-tests passes"
  artifacts:
    - path: "Tests/InvariantSwiftTests/TestHelpers/DiffAssertions.swift"
      provides: "Test assertion helpers for diff comparison"
      exports: ["expectNoDifference", "expectDifference"]
  key_links:
    - from: "Tests/InvariantSwiftTests/FunctionalTesting/PrettyPrinterEnhancementTests.swift"
      to: "Tests/InvariantSwiftTests/TestHelpers/DiffAssertions.swift"
      via: "@testable import InvariantSwift"
      pattern: "expectNoDifference"
---

<objective>
Implement expectNoDifference and expectDifference test helpers to enable PrettyPrinterEnhancementTests.swift compilation.

Purpose: PrettyPrinterEnhancementTests.swift (lines 295-315) references expectNoDifference and expectDifference functions that do not exist. Swift Testing .disabled() trait does NOT prevent compilation of test bodies, causing build failures.

Output: DiffAssertions.swift in Tests/InvariantSwiftTests/TestHelpers/ with diff assertion helpers.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Tests/InvariantSwiftTests/FunctionalTesting/PrettyPrinterEnhancementTests.swift
@Sources/InvariantSwift/Presentation/PrettyPrint.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestHelpers directory and DiffAssertions.swift</name>
  <files>Tests/InvariantSwiftTests/TestHelpers/DiffAssertions.swift</files>
  <action>
Create Tests/InvariantSwiftTests/TestHelpers/ directory and DiffAssertions.swift with diff assertion helpers.

From PrettyPrinterEnhancementTests.swift analysis:
- Line 295: `expectNoDifference(actual, expected)`
- Line 303: `expectDifference(actual, expected)`

Required API:
```swift
import Testing
@testable import InvariantSwift

/// Assert that two values produce no structural differences
///
/// Uses PrettyPrint.diff to compare values and fails if differences found.
public func expectNoDifference<T: Equatable>(
  _ actual: T,
  _ expected: T,
  sourceLocation: SourceLocation = #_sourceLocation
) {
  guard actual != expected else { return }

  Issue.record(
    "Expected no differences but found changes",
    sourceLocation: sourceLocation
  )
}

/// Assert that two values produce structural differences
///
/// Uses PrettyPrint.diff to compare values and fails if NO differences found.
public func expectDifference<T: Equatable>(
  _ actual: T,
  _ expected: T,
  sourceLocation: SourceLocation = #_sourceLocation
) {
  guard actual == expected else { return }

  Issue.record(
    "Expected differences but values are equal",
    sourceLocation: sourceLocation
  )
}
```

Minimal implementation using Swift Testing Issue.record. Can be enhanced later with custom-dump integration.
  </action>
  <verify>
1. TestHelpers directory exists
2. `swift build --build-tests` compiles DiffAssertions.swift
3. expectNoDifference and expectDifference are accessible from test files
  </verify>
  <done>
DiffAssertions.swift exists with expectNoDifference and expectDifference helpers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enable diff assertion tests in PrettyPrinterEnhancementTests.swift</name>
  <files>Tests/InvariantSwiftTests/FunctionalTesting/PrettyPrinterEnhancementTests.swift</files>
  <action>
Now that expectNoDifference and expectDifference exist, remove the .disabled() traits from tests using them.

1. Find tests using expectNoDifference/expectDifference (around lines 295-315)

2. Remove `.disabled("expectNoDifference not yet implemented")` traits

3. Verify tests compile and run:
   ```bash
   swift build --build-tests
   swift test --filter PrettyPrinterEnhancementTests
   ```

Expected outcome: Tests compile and execute (may pass or fail, but must compile).
  </action>
  <verify>
1. `swift build --build-tests` passes
2. `swift test --filter PrettyPrinterEnhancementTests` runs without compilation errors
3. No .disabled() traits remain on diff assertion tests
  </verify>
  <done>
Diff assertion tests enabled and compiling successfully.
  </done>
</task>

</tasks>

<verification>
1. `swift build --build-tests` - test target compiles
2. `swift test --filter PrettyPrinterEnhancementTests` - tests run without compilation errors
3. expectNoDifference and expectDifference accessible from test code
4. No compilation errors referencing expectNoDifference or expectDifference
</verification>

<success_criteria>
1. Tests/InvariantSwiftTests/TestHelpers/DiffAssertions.swift exists
2. expectNoDifference and expectDifference functions implemented
3. PrettyPrinterEnhancementTests.swift diff assertion tests enabled (no .disabled() traits)
4. swift build --build-tests passes
5. No compilation errors in test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-22-SUMMARY.md`
</output>
