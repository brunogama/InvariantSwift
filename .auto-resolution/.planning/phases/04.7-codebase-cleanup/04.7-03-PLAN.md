---
phase: 04.7-codebase-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["04.7-01", "04.7-02"]
files_modified:
  - Tests/InvariantSwiftMacroTests/PropertyMacroTests.swift
  - Tests/InvariantSwiftMacroTests/BusinessRuleMacroTests.swift
  - Tests/InvariantSwiftMacroTests/LawCheckedMacroTests.swift
  - Tests/InvariantSwiftMacroTests/StateMachineMacroTests.swift
  - Tests/InvariantSwiftMacroTests/PropertyTestMacroClassificationTests.swift
  - Tests/InvariantSwiftMacroTests/TimeoutMacroTests.swift
autonomous: true

must_haves:
  truths:
    - "Developer can run `swiftlint lint --strict Tests/InvariantSwiftMacroTests/` and see zero errors"
    - "Developer can run `swift build --target InvariantSwiftMacroTests` and see build succeed"
    - "Code review shows no blanket disable commands (all disables are targeted with `:next`/`:this`)"
    - "Reflexivity tests (value == value) are clearly documented as intentional"
  artifacts:
    - path: "Tests/InvariantSwiftMacroTests/"
      provides: "Clean macro test suite"
  key_links:
    - from: "swiftlint lint --strict"
      to: "Tests/InvariantSwiftMacroTests/"
      via: "zero violations"
---

<objective>
Fix SwiftLint violations in InvariantSwiftMacroTests

Purpose: Clean up the macro test files which have the highest violation counts (PropertyMacroTests: 21, BusinessRuleMacroTests: 16). These are primarily closure_parameter_position, identical_operands (intentional reflexivity tests), and file length issues.
Output: Zero unjustified SwiftLint violations in macro tests
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-codebase-cleanup/04.7-RESEARCH.md
@RULES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PropertyMacroTests and BusinessRuleMacroTests</name>
  <files>
    Tests/InvariantSwiftMacroTests/PropertyMacroTests.swift
    Tests/InvariantSwiftMacroTests/BusinessRuleMacroTests.swift
  </files>
  <action>
Fix the 37 violations in these two files:

1. PropertyMacroTests.swift (21 violations):
   - Run `swiftlint lint --strict Tests/InvariantSwiftMacroTests/PropertyMacroTests.swift 2>&1` to see specific issues

   Common fixes needed:
   a) closure_parameter_position: Move closure params to same line as brace
      ```swift
      // BEFORE
      assertMacroExpansion(
        ...,
        macros: testMacros
      ) {
        param in
        ...
      }

      // AFTER
      assertMacroExpansion(
        ...,
        macros: testMacros
      ) { param in
        ...
      }
      ```

   b) identical_operands (reflexivity tests): Add targeted disable with comment
      ```swift
      // Testing reflexivity property - intentional identical operands
      // swiftlint:disable:next identical_operands
      #expect(value == value)
      ```

   c) line_length: Break long macro expansion strings across lines using string concatenation

2. BusinessRuleMacroTests.swift (16 violations):
   - Same patterns as PropertyMacroTests
   - Focus on closure_parameter_position and line_length
  </action>
  <verify>
Run: `swiftlint lint --strict Tests/InvariantSwiftMacroTests/PropertyMacroTests.swift Tests/InvariantSwiftMacroTests/BusinessRuleMacroTests.swift 2>&1 | grep error: | wc -l`
Expected: 0
  </verify>
  <done>PropertyMacroTests and BusinessRuleMacroTests have zero violations</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining macro test files</name>
  <files>
    Tests/InvariantSwiftMacroTests/LawCheckedMacroTests.swift
    Tests/InvariantSwiftMacroTests/StateMachineMacroTests.swift
    Tests/InvariantSwiftMacroTests/PropertyTestMacroClassificationTests.swift
    Tests/InvariantSwiftMacroTests/TimeoutMacroTests.swift
  </files>
  <action>
Fix violations in remaining macro test files:

1. LawCheckedMacroTests.swift (6 violations):
   - Remove superfluous type_body_length disable if type is now under limit
   - Fix closure_parameter_position violations

2. StateMachineMacroTests.swift (3 violations):
   - File length: If over 1000 lines, either split into StateMachineMacroTests+Extensions.swift
     OR add justified disable: "// Comprehensive macro expansion tests require cohesive file"
   - Remove superfluous disables

3. PropertyTestMacroClassificationTests.swift (3 violations):
   - Fix closure_parameter_position
   - Remove superfluous disables

4. TimeoutMacroTests.swift:
   - Remove any superfluous disables
   - Fix formatting issues

Pattern for file length decisions:
- If tests are logically related: Keep together with justified disable
- If tests have natural groupings: Split into separate files (e.g., by test category)
- Threshold: 1000 lines is error, try to stay under 800 for comfort
  </action>
  <verify>
Run: `swiftlint lint --strict Tests/InvariantSwiftMacroTests/ 2>&1 | grep error: | wc -l`
Expected: 0
  </verify>
  <done>All InvariantSwiftMacroTests have zero violations</done>
</task>

<task type="auto">
  <name>Task 3: Verify macro tests compile and run</name>
  <files>
    Tests/InvariantSwiftMacroTests/
  </files>
  <action>
Verify that macro tests still compile and can run after cleanup:

1. Build test target:
   ```bash
   swift build --target InvariantSwiftMacroTests
   ```

2. If build fails, fix any issues introduced during cleanup:
   - Imports that were accidentally removed
   - Code that was incorrectly refactored
   - Missing dependencies

3. Run a quick sanity test (may fail due to SPM cache issues, but should compile):
   ```bash
   swift test --filter InvariantSwiftMacroTests --parallel 2>&1 | head -50
   ```

4. Final lint check:
   ```bash
   swiftlint lint --strict Tests/InvariantSwiftMacroTests/
   ```
  </action>
  <verify>
Run: `swift build --target InvariantSwiftMacroTests 2>&1 | tail -5`
Expected: "Build complete!" or equivalent success message

Run: `swiftlint lint --strict Tests/InvariantSwiftMacroTests/ 2>&1 | grep error: | wc -l`
Expected: 0
  </verify>
  <done>InvariantSwiftMacroTests compiles successfully and has zero SwiftLint violations</done>
</task>

</tasks>

<verification>
Overall verification for Plan 03:

1. SwiftLint: `swiftlint lint --strict Tests/InvariantSwiftMacroTests/ 2>&1 | grep error: | wc -l` = 0
2. Build: `swift build --target InvariantSwiftMacroTests` succeeds
3. No blanket disables: `rg "swiftlint:disable [a-z]" Tests/InvariantSwiftMacroTests/ | grep -v ":next\|:this\|:previous" | wc -l` = 0
</verification>

<success_criteria>
- All files in Tests/InvariantSwiftMacroTests/ have zero SwiftLint violations
- Macro tests compile successfully
- All disables are targeted (`:next`, `:this`, `:previous`) with justification comments
- Identical operands violations properly marked as intentional reflexivity tests
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-03-SUMMARY.md`
</output>
