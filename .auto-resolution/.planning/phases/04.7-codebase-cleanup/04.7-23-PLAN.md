---
phase: 04.7-codebase-cleanup
plan: 23
type: execute
wave: 3
depends_on: [21, 22]
files_modified:
  - Sources/InvariantSwift/Core/Gen.swift
  - Sources/InvariantSwift/Core/Shrink.swift
  - Sources/InvariantSwift/Core/SizeType.swift
  - Sources/InvariantSwift/Core/SeedType.swift
  - Sources/InvariantSwift/Core/Generator.swift
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Generator.swift file_length is under 1000 lines OR file is split into modules"
    - "No swiftlint:disable file_length markers remain"
    - "All Gen/Shrink/Size/Seed functionality preserved"
    - "swift build -Xswiftc -warnings-as-errors passes"
    - "swift test passes without failures"
  artifacts:
    - path: "Sources/InvariantSwift/Core/Gen.swift"
      provides: "Gen<T> type and core generator combinators"
    - path: "Sources/InvariantSwift/Core/Shrink.swift"
      provides: "Shrink<T> type and shrinking strategies"
    - path: "Sources/InvariantSwift/Core/SizeType.swift"
      provides: "Size type for generation bounds"
    - path: "Sources/InvariantSwift/Core/SeedType.swift"
      provides: "Seed type for deterministic generation"
---

<objective>
Refactor Generator.swift to remove file_length SwiftLint disable by splitting into focused modules.

Purpose: User requirement states "goal is NO disable rules - do the refactorings needed". Generator.swift is 1691 lines (exceeds 1000-line limit) and currently uses documented blanket file_length disable. Must split into Gen/Shrink/Size/Seed modules to comply with linting rules.

Output: Generator.swift split into 4 focused files, each under 400 lines, with zero SwiftLint disables.
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Sources/InvariantSwift/Core/Generator.swift
@Package.swift
@.swiftlint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Gen<T> type to Gen.swift</name>
  <files>Sources/InvariantSwift/Core/Gen.swift</files>
  <action>
Extract Gen<T> struct and all generator combinators to Gen.swift.

From Generator.swift, extract:
- Lines ~30-800: Gen<T> struct definition
- All static generator methods (int, string, array, etc.)
- All instance methods (map, flatMap, filter, etc.)
- Generator composition combinators

Target: ~600 lines (under 1000-line error limit, ideally under 400-line warning limit)

Keep imports minimal:
```swift
import Foundation

/// Core generator type for property-based testing
public struct Gen<T>: @unchecked Sendable {
  // ... implementation
}
```

Ensure all public APIs remain public, all internal APIs remain internal.
  </action>
  <verify>
1. Gen.swift compiles successfully
2. `swiftlint lint --strict Sources/InvariantSwift/Core/Gen.swift` shows 0 errors
3. File is under 1000 lines (ideally under 400)
  </verify>
  <done>
Gen.swift exists with Gen<T> type and combinators, zero SwiftLint violations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract Shrink<T> type to Shrink.swift</name>
  <files>Sources/InvariantSwift/Core/Shrink.swift</files>
  <action>
Extract Shrink<T> struct and all shrinking strategies to Shrink.swift.

From Generator.swift, extract:
- Lines ~800-1200: Shrink<T> struct definition
- All shrinking functions (shrinkInt, shrinkArray, shrinkTowards, etc.)
- Shrinking combinators

Target: ~400 lines

Keep imports minimal:
```swift
import Foundation

/// Shrinking strategy for generating simpler counterexamples
public struct Shrink<T>: @unchecked Sendable {
  // ... implementation
}
```
  </action>
  <verify>
1. Shrink.swift compiles successfully
2. `swiftlint lint --strict Sources/InvariantSwift/Core/Shrink.swift` shows 0 errors
3. File is under 400 lines
  </verify>
  <done>
Shrink.swift exists with Shrink<T> type and strategies, zero SwiftLint violations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract Size type to SizeType.swift</name>
  <files>Sources/InvariantSwift/Core/SizeType.swift</files>
  <action>
Extract Size struct to SizeType.swift.

From Generator.swift, extract:
- Lines ~1200-1400: Size struct definition
- Size scaling and clamping functions

Target: ~200 lines

Note: Cannot name file Size.swift because that conflicts with Size+Lenses.swift pattern. Use SizeType.swift instead.

```swift
import Foundation

/// Size parameter for controlling test data generation bounds
public struct Size: Equatable, Hashable, Sendable {
  // ... implementation
}
```
  </action>
  <verify>
1. SizeType.swift compiles successfully
2. `swiftlint lint --strict Sources/InvariantSwift/Core/SizeType.swift` shows 0 errors
3. File is under 400 lines
  </verify>
  <done>
SizeType.swift exists with Size type, zero SwiftLint violations.
  </done>
</task>

<task type="auto">
  <name>Task 4: Extract Seed type to SeedType.swift</name>
  <files>Sources/InvariantSwift/Core/SeedType.swift</files>
  <action>
Extract Seed struct to SeedType.swift.

From Generator.swift, extract:
- Lines ~1400-1691: Seed struct definition
- Random number generation functions
- Seed creation and manipulation

Target: ~300 lines

Note: Cannot name file Seed.swift because that conflicts with Seed+Lenses.swift pattern. Use SeedType.swift instead.

```swift
import Foundation

/// Seed for deterministic random number generation
public struct Seed: Equatable, Hashable, Sendable {
  // ... implementation
}
```
  </action>
  <verify>
1. SeedType.swift compiles successfully
2. `swiftlint lint --strict Sources/InvariantSwift/Core/SeedType.swift` shows 0 errors
3. File is under 400 lines
  </verify>
  <done>
SeedType.swift exists with Seed type, zero SwiftLint violations.
  </done>
</task>

<task type="auto">
  <name>Task 5: Remove Generator.swift and update Package.swift</name>
  <files>Sources/InvariantSwift/Core/Generator.swift, Package.swift</files>
  <action>
After extracting all types, Generator.swift should be empty or nearly empty.

1. Delete Generator.swift:
   ```bash
   git rm Sources/InvariantSwift/Core/Generator.swift
   ```

2. Verify no Package.swift changes needed (sources auto-discovered)

3. Run full build:
   ```bash
   swift build -Xswiftc -warnings-as-errors
   ```

4. Run tests:
   ```bash
   swift test
   ```

Expected outcome: All builds and tests pass, Generator.swift deleted, no file_length disables remain.
  </action>
  <verify>
1. Generator.swift deleted from filesystem
2. `swift build -Xswiftc -warnings-as-errors` passes
3. `swift test` passes with 100% success rate
4. No swiftlint:disable file_length markers in codebase
  </verify>
  <done>
Generator.swift deleted, all types split into focused modules, zero SwiftLint disables, all tests pass.
  </done>
</task>

</tasks>

<verification>
1. `find Sources/InvariantSwift/Core/ -name "*.swift" | xargs wc -l` - all files under 1000 lines
2. `grep -r "swiftlint:disable file_length" Sources/` - no matches
3. `swift build -Xswiftc -warnings-as-errors` - passes
4. `swift test` - 100% success rate
5. `swiftlint lint --strict Sources/InvariantSwift/Core/` - 0 errors
</verification>

<success_criteria>
1. Gen.swift exists (under 1000 lines, ideally under 400)
2. Shrink.swift exists (under 400 lines)
3. SizeType.swift exists (under 400 lines)
4. SeedType.swift exists (under 400 lines)
5. Generator.swift deleted
6. No swiftlint:disable file_length markers remain in codebase
7. All functionality preserved (tests pass)
8. Zero SwiftLint violations
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-23-SUMMARY.md`
</output>
