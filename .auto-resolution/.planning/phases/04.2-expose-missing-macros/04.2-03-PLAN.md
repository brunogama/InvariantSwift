---
phase: 04.2-expose-missing-macros
plan: 03
type: execute
wave: 2
depends_on:
  - 04.2-01
  - 04.2-02
files_modified:
  - docs/MACROS.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "docs/MACROS.md accurately reflects @LawChecked and @DeriveGen availability"
    - "Documentation shows correct import statements"
    - "Examples in docs compile with zero errors"
  artifacts:
    - path: "docs/MACROS.md"
      provides: "Accurate macro documentation"
      contains: "@LawChecked"
  key_links:
    - from: "docs/MACROS.md @LawChecked section"
      to: "Sources/InvariantSwift/Macros/LawCheckedMacroDeclaration.swift"
      via: "documented API matches declaration"
      pattern: "@LawChecked"
    - from: "docs/MACROS.md @DeriveGen section"
      to: "Sources/InvariantSwift/Macros/DeriveGenMacroDeclaration.swift"
      via: "documented API matches declaration"
      pattern: "@DeriveGen"
---

<objective>
Update docs/MACROS.md to accurately reflect the @LawChecked and @DeriveGen macro availability after Plans 01 and 02 complete.

Purpose: The documentation currently describes @LawChecked and @DeriveGen usage, but users get "Cannot find in scope" errors when trying to use them. After Plans 01 and 02 expose these macros, the documentation needs verification and minor updates to ensure accuracy.

Output: Accurate documentation that matches the actual public API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-expose-missing-macros/04.2-RESEARCH.md
@.planning/phases/04.2-expose-missing-macros/04.2-01-SUMMARY.md
@.planning/phases/04.2-expose-missing-macros/04.2-02-SUMMARY.md
@docs/MACROS.md
@Sources/InvariantSwift/Macros/LawCheckedMacroDeclaration.swift
@Sources/InvariantSwift/Macros/DeriveGenMacroDeclaration.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and update @LawChecked documentation section</name>
  <files>docs/MACROS.md</files>
  <action>
Verify the @LawChecked section (lines 270-330) in docs/MACROS.md matches the actual declaration in LawCheckedMacroDeclaration.swift.

**Verification checklist:**
1. Parameter names and types match declaration
2. Default values match declaration
3. Example code compiles (test in Xcode or swift build)
4. MathematicalLaw enum values match declaration
5. Import statement is correct

**If discrepancies found, update the documentation to match the declaration.**

Typical issues to check:
- Parameter order
- Default values
- Enum case names
- Generic constraints in examples
- Required protocol conformances

**Expected parameters from declaration:**
- `laws: [MathematicalLaw] = []`
- `customLaws: [String: String] = [:]`
- `iterations: Int = 100`
- `size: Int = 50`
- `enableShrinking: Bool = true`
- `timeout: Double = 30.0`

**Add availability note if missing:**
```markdown
> **Availability:** InvariantSwift 2.0+
>
> Import: `import InvariantSwift`
```
  </action>
  <verify>
Read docs/MACROS.md @LawChecked section and compare with LawCheckedMacroDeclaration.swift.
All parameters match.
All MathematicalLaw cases documented.
  </verify>
  <done>@LawChecked documentation accurately reflects the public API.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update @DeriveGen documentation section</name>
  <files>docs/MACROS.md</files>
  <action>
Verify the @DeriveGen section (lines 333-400+) in docs/MACROS.md matches the actual declaration in DeriveGenMacroDeclaration.swift.

**Verification checklist:**
1. Parameter names and types match declaration
2. Default values match declaration
3. Example code compiles (test in Xcode or swift build)
4. Import statement is correct

**If discrepancies found, update the documentation to match the declaration.**

Typical issues to check:
- Parameter order
- Default values
- Generated member names (gen vs arbitrary)
- Generatable conformance mention

**Expected parameters from declaration:**
- `customFields: [String: String] = [:]`
- `maxDepth: Int = 5`
- `sizeScaling: Double = 1.0`
- `enableShrinking: Bool = true`

**Add availability note if missing:**
```markdown
> **Availability:** InvariantSwift 2.0+
>
> Import: `import InvariantSwift`
```

**Clarify difference from @Arbitrary:**
- `@Arbitrary` generates `arbitrary` property and is the primary macro for most use cases
- `@DeriveGen` generates `gen` property and offers more configuration options
- Document when to use each
  </action>
  <verify>
Read docs/MACROS.md @DeriveGen section and compare with DeriveGenMacroDeclaration.swift.
All parameters match.
Difference from @Arbitrary explained.
  </verify>
  <done>@DeriveGen documentation accurately reflects the public API.</done>
</task>

<task type="auto">
  <name>Task 3: Run documentation code examples to verify compilation</name>
  <files>docs/MACROS.md</files>
  <action>
Extract code examples from the @LawChecked and @DeriveGen sections and verify they compile.

**Method:**
1. Create a temporary test file with the examples
2. Run `swift build` to verify compilation
3. Fix any compilation errors in the documentation

**Example verification script:**
```bash
# Create temporary file with examples
cat > /tmp/MacroDocTest.swift << 'EOF'
import InvariantSwift

// @LawChecked example from docs
@LawChecked(laws: [.functor])
struct MyBox<T>: Functor, Equatable {
    let value: T
    func map<U>(_ f: (T) -> U) -> MyBox<U> { MyBox<U>(value: f(value)) }
}

// @DeriveGen example from docs
@DeriveGen
struct Person {
    let name: String
    let age: Int
    let email: String?
}
EOF

# Build to verify
swift build
```

If examples fail to compile, update docs/MACROS.md with correct syntax.

**Common fixes needed:**
- Protocol conformances missing (Equatable, Functor, etc.)
- Static gen/arbitrary property not shown correctly
- Missing Generatable conformance mention
  </action>
  <verify>
Documentation examples compile without errors when tested.
  </verify>
  <done>All code examples in docs/MACROS.md are verified to compile correctly.</done>
</task>

</tasks>

<verification>
1. Compare docs/MACROS.md @LawChecked section with LawCheckedMacroDeclaration.swift
2. Compare docs/MACROS.md @DeriveGen section with DeriveGenMacroDeclaration.swift
3. All parameters, types, and defaults match
4. Code examples compile when extracted and tested
5. Zero markdown lint warnings (if applicable)
</verification>

<success_criteria>
- docs/MACROS.md accurately describes @LawChecked parameters and usage
- docs/MACROS.md accurately describes @DeriveGen parameters and usage
- All MathematicalLaw enum cases are documented
- Code examples compile without modification
- Users following the documentation can successfully use both macros
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-expose-missing-macros/04.2-03-SUMMARY.md`
</output>
