---
phase: 04.3-equivalence-macro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift
  - Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceMacro.swift
  - Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceConfigExtractor.swift
  - Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceDiagnostic.swift
  - Sources/InvariantSwiftMacros/MacroPlugin.swift
autonomous: true

must_haves:
  truths:
    - "@Equivalence macro compiles without errors"
    - "@Equivalence generates test comparing reference vs candidate functions"
    - "Tolerance parameter enables floating-point approximate equality"
    - "Macro emits diagnostic when applied to non-function"
    - "Macro emits diagnostic when function has wrong parameter count"
    - "Macro emits diagnostic when tolerance specified for non-floating-point output type"
  artifacts:
    - path: "Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift"
      provides: "Public macro declaration with iterations and tolerance parameters"
      contains: "@attached(peer, names: suffixed(_EquivalenceTest))"
      min_lines: 30
    - path: "Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceMacro.swift"
      provides: "Peer macro implementation generating Swift Testing @Test"
      exports: ["EquivalenceMacro"]
      min_lines: 150
    - path: "Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceConfigExtractor.swift"
      provides: "Config extraction from macro attributes"
      min_lines: 40
    - path: "Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceDiagnostic.swift"
      provides: "Diagnostic messages for error cases"
      min_lines: 25
    - path: "Sources/InvariantSwiftMacros/MacroPlugin.swift"
      provides: "Macro registration"
      contains: "EquivalenceMacro.self"
  key_links:
    - from: "Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift"
      to: "Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceMacro.swift"
      via: "#externalMacro reference"
      pattern: "type:\\s*\"EquivalenceMacro\""
    - from: "Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceMacro.swift"
      to: "Sources/InvariantSwiftMacros/MacroPlugin.swift"
      via: "plugin registration"
      pattern: "EquivalenceMacro\\.self"
---

<objective>
Implement the @Equivalence macro core infrastructure for function equivalence testing.

Purpose: Enable developers to verify two function implementations produce identical results, supporting safe refactoring and algorithm comparison.

Output: Working @Equivalence macro that generates Swift Testing @Test functions comparing reference vs candidate implementations with optional floating-point tolerance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-equivalence-macro/04.3-RESEARCH.md
@Sources/InvariantSwiftMacros/PropertyMacro/PropertyMacro.swift
@Sources/InvariantSwiftMacros/DifferentialTestMacro.swift
@Sources/InvariantSwift/Macros/PropertyMacroDeclaration.swift
@Sources/InvariantSwift/Differential/DifferentialTesting.swift
@Sources/InvariantSwift/Generators/FloatingPointMode.swift
@Sources/InvariantSwiftMacros/Utilities/ParameterExtractor.swift
@Sources/InvariantSwiftMacros/Utilities/GeneratorInference.swift
@Sources/InvariantSwiftMacros/Utilities/TypeAnalyzer.swift
@Sources/InvariantSwiftMacros/Utilities/FunctionCallBuilder.swift
@Sources/InvariantSwiftMacros/Utilities/SyntaxFactory.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create macro declaration and diagnostic types</name>
  <files>
    Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift
    Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceDiagnostic.swift
    Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceConfigExtractor.swift
  </files>
  <action>
    Create the public macro declaration in Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift:

    1. Define @Equivalence as a peer macro with names: suffixed(_EquivalenceTest)
    2. Parameters:
       - iterations: Int = 500 (default number of test iterations)
       - tolerance: Double? = nil (optional for floating-point comparison)
    3. Reference #externalMacro(module: "InvariantSwiftMacros", type: "EquivalenceMacro")
    4. Add comprehensive documentation showing:
       - Basic usage: comparing two sort implementations
       - Tolerance usage: comparing floating-point calculations
       - When to use: refactoring validation, optimization verification

    Create Sources/InvariantSwiftMacros/EquivalenceMacro/ directory.

    Create EquivalenceDiagnostic.swift with MacroDiagnosticMessage conformance:
    - mustBeFunction: "@Equivalence can only be applied to functions"
    - requiresTwoFunctionParameters: "@Equivalence requires exactly two function parameters (reference, candidate)"
    - incompatibleFunctionTypes: "Reference and candidate functions must have matching signatures"
    - toleranceRequiresBinaryFloatingPoint: "tolerance parameter requires Output type to conform to BinaryFloatingPoint"

    Create EquivalenceConfigExtractor.swift:
    - EquivalenceMacroConfig struct with iterations: Int, tolerance: Double?
    - Static extract(from: AttributeSyntax) -> EquivalenceMacroConfig
    - Parse iterations and tolerance from LabeledExprListSyntax

    Follow PropertyMacroDeclaration.swift pattern for imports and documentation style.
    Use SwiftSyntax for config extraction (not string parsing).
  </action>
  <verify>
    swift build -Xswiftc -warnings-as-errors 2>&1 | head -50
    # Check files exist:
    ls -la Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift
    ls -la Sources/InvariantSwiftMacros/EquivalenceMacro/
  </verify>
  <done>
    - EquivalenceMacroDeclaration.swift exists with @attached(peer) macro definition
    - EquivalenceDiagnostic.swift provides 4 diagnostic message types
    - EquivalenceConfigExtractor.swift extracts iterations and tolerance from attributes
    - Build succeeds (macro declaration visible to library consumers)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement EquivalenceMacro peer macro</name>
  <files>
    Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceMacro.swift
    Sources/InvariantSwiftMacros/MacroPlugin.swift
  </files>
  <action>
    Create Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceMacro.swift implementing PeerMacro:

    1. Import SwiftCompilerPlugin, SwiftSyntax, SwiftSyntaxBuilder, SwiftSyntaxMacros
       Also import the local module files:
       - EquivalenceConfigExtractor (from same directory)
       - EquivalenceDiagnostic (from same directory)

    2. Implement EquivalenceMacro: PeerMacro with expansion function:
       - Validate declaration is FunctionDeclSyntax (emit mustBeFunction diagnostic if not)
       - Extract function parameters using ParameterExtractor
       - Validate exactly 2 parameters exist (emit requiresTwoFunctionParameters if not)
       - Both parameters should be function types with matching signatures
       - Extract config using EquivalenceConfigExtractor.extract(from: node)

    3. TOLERANCE TYPE VALIDATION (CRITICAL - addresses blocker #1):
       When config.tolerance is not nil, validate the output type:
       - Extract output type from the function parameters' return type annotation
         (the return type of the reference/candidate function type, e.g., `[Int]` from `([Int]) -> [Int]`)
       - Use TypeAnalyzer.baseTypeName(from: outputType) to get type name
       - Check against known BinaryFloatingPoint types: ["Double", "Float", "Float16", "Float80", "CGFloat"]
       - If output type is NOT in this set, emit toleranceRequiresBinaryFloatingPoint diagnostic via context.diagnose()
       - Return empty array after emitting diagnostic (do not generate code)

    4. GENERATOR INFERENCE (CRITICAL - addresses blocker #2):
       Build input generator using GeneratorInference utility:
       - Extract input type(s) from reference function parameter signature
         (e.g., for `(reference: @escaping ([Int]) -> [Int])`, extract the function type,
          then get its input parameter types)
       - For each input type, call GeneratorInference.infer(for: inputTypeSyntax) to get Gen<Type> expression
       - If multiple input parameters, combine with FunctionCallBuilder.genZip([gen1, gen2, ...]).buildExpr()
       - If single input parameter, use the inferred generator directly
       - Store generator expression as ExprSyntax for use in test body

    5. Build wrapper enum pattern (like PropertyMacro):
       - Enum name: "{functionName}_EquivalenceTest"
       - Contains static @Test function named "run"
       - Function signature: static func run() throws

    6. Build test body using pure AST builders (NO string interpolation):
       - Use iteration count from config for test loop
       - Call reference(input) and candidate(input) using FunctionCallBuilder
       - Compare results based on tolerance:

         WITHOUT TOLERANCE (config.tolerance == nil):
         Build: `if referenceResult != candidateResult { Issue.record(...) }`
         - Use InfixOperatorExprSyntax with "!=" operator
         - Build Issue.record call using FunctionCallBuilder

         WITH TOLERANCE (CRITICAL - addresses blocker #3):
         Build FloatingPointTolerance comparison using instance method API:
         ```
         referenceResult.isApproximatelyEqual(to: candidateResult, tolerance: .absolute(tolerance))
         ```

         AST Construction Steps:
         a. Build tolerance enum value `.absolute(tolerance)`:
            - Use MemberAccessExprSyntax for base ".absolute"
            - Wrap in FunctionCallExprSyntax with tolerance value (FloatLiteralExprSyntax) as argument
            - Result: toleranceValue (ExprSyntax)

         b. Build instance method call `referenceResult.isApproximatelyEqual(to:tolerance:)`:
            - Base: DeclReferenceExprSyntax("referenceResult")
            - Member: MemberAccessExprSyntax(base: referenceResult, member: "isApproximatelyEqual")
            - Use FunctionCallExprSyntax with labeled arguments:
              - LabeledExprSyntax(label: "to", expression: DeclReferenceExprSyntax("candidateResult"))
              - LabeledExprSyntax(label: "tolerance", expression: toleranceValue from step a)
            - Result: isApproxEqualExpr (ExprSyntax)

         c. Build negation for divergence check:
            - Use PrefixOperatorExprSyntax with "!" operator on isApproxEqualExpr
            - Result: divergenceCondition (ExprSyntax)

         d. Build if statement:
            - IfExprSyntax with condition = divergenceCondition
            - Body = CodeBlockSyntax containing Issue.record call

       - On divergence: Issue.record with formatted message showing input, reference result, candidate result

    7. Handle error cases with proper diagnostics emitted via context.diagnose()

    Follow PropertyMacro.swift patterns for:
    - FunctionDeclSyntax construction
    - EnumDeclSyntax wrapper pattern
    - AttributeListSyntax for @Test
    - CodeBlockSyntax for test body

    CRITICAL: Use AST builders exclusively. Never use DeclSyntax(stringLiteral:).

    Register EquivalenceMacro.self in MacroPlugin.swift providingMacros array.
  </action>
  <verify>
    swift build -Xswiftc -warnings-as-errors
    # Verify macro is registered:
    grep -n "EquivalenceMacro.self" Sources/InvariantSwiftMacros/MacroPlugin.swift
    # Check implementation exists:
    wc -l Sources/InvariantSwiftMacros/EquivalenceMacro/EquivalenceMacro.swift
  </verify>
  <done>
    - EquivalenceMacro.swift implements PeerMacro protocol
    - Validates tolerance only used with BinaryFloatingPoint output types (Double, Float, Float16, Float80, CGFloat)
    - Uses GeneratorInference.infer(for:) for input type generator building
    - Generates wrapper enum with @Test static function
    - Uses pure AST builders (no string interpolation)
    - Builds FloatingPointTolerance.absolute(x).isApproximatelyEqual(a, b) via nested MemberAccessExprSyntax and FunctionCallExprSyntax
    - Supports optional tolerance parameter for floating-point comparison
    - Emits proper diagnostics for error cases
    - Registered in MacroPlugin.swift
    - Build succeeds with zero warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verification and lint check</name>
  <files>
    (no new files - verification only)
  </files>
  <action>
    Run full build and lint verification:

    1. Run swift build with warnings-as-errors
    2. Run swiftlint on new files
    3. Verify macro registration by inspecting MacroPlugin.swift
    4. Test basic macro expansion by creating a minimal test case

    If any issues found, fix them before completing this task.
  </action>
  <verify>
    swift build -Xswiftc -warnings-as-errors
    swiftlint lint --strict Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift Sources/InvariantSwiftMacros/EquivalenceMacro/
  </verify>
  <done>
    - Full build passes with zero warnings
    - SwiftLint reports zero violations
    - Macro files follow project conventions
    - Ready for test suite in Plan 02
  </done>
</task>

</tasks>

<verification>
1. Build verification:
   swift build -Xswiftc -warnings-as-errors

2. Lint verification:
   swiftlint lint --strict Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift
   swiftlint lint --strict Sources/InvariantSwiftMacros/EquivalenceMacro/

3. Files exist verification:
   ls -la Sources/InvariantSwift/Macros/EquivalenceMacroDeclaration.swift
   ls -la Sources/InvariantSwiftMacros/EquivalenceMacro/

4. Macro registration verification:
   grep "EquivalenceMacro.self" Sources/InvariantSwiftMacros/MacroPlugin.swift
</verification>

<success_criteria>
- @Equivalence macro declaration exists with iterations and tolerance parameters
- EquivalenceMacro implementation generates @Test wrapper enum
- Tolerance validated against BinaryFloatingPoint types before code generation
- Generator inference uses GeneratorInference.infer(for:) utility
- FloatingPointTolerance AST built via nested MemberAccessExprSyntax and FunctionCallExprSyntax
- Build succeeds with zero warnings
- SwiftLint passes with zero violations
- Macro registered in MacroPlugin.swift
- Code follows PropertyMacro pattern (pure AST builders, proper diagnostics)
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-equivalence-macro/04.3-01-SUMMARY.md`
</output>
