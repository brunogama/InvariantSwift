---
phase: 07-roundtrip
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift
  - Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift
autonomous: true

must_haves:
  truths:
    - "Macro expansion tests verify correct AST generation for each strategy"
    - "Integration tests verify roundtrip tests actually pass for valid Codable types"
    - "Error diagnostics are emitted for invalid usage"
    - "Tests cover all three strategies: json, plist, hash"
  artifacts:
    - path: "Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift"
      provides: "Macro expansion tests"
      min_lines: 150
    - path: "Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift"
      provides: "Integration tests with real types"
      min_lines: 80
  key_links:
    - from: "Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift"
      to: "RoundtripMacro"
      via: "assertMacroExpansion"
      pattern: "assertMacroExpansion"
    - from: "Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift"
      to: "@Roundtrip"
      via: "macro usage"
      pattern: "@Roundtrip"
---

<objective>
Create comprehensive tests for the @Roundtrip macro covering expansion verification, integration with real Codable types, and error diagnostics.

Purpose: Ensure @Roundtrip macro generates correct property tests and handles edge cases gracefully.

Output: Test suite with 100% coverage of macro functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-roundtrip/07-RESEARCH.md
@.planning/phases/07-roundtrip/07-01-SUMMARY.md
@Tests/InvariantSwiftMacroTests/PropertyMacroTests.swift
@Sources/InvariantSwiftMacros/RoundtripMacro/RoundtripMacro.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create macro expansion tests</name>
  <files>
    Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift
  </files>
  <action>
Create comprehensive macro expansion tests using SwiftSyntaxMacrosTestSupport.

Follow PropertyMacroTests.swift pattern:

```swift
import SwiftSyntaxMacrosTestSupport
import Testing
@testable import InvariantSwiftMacros

let roundtripMacros: [String: Macro.Type] = [
  "Roundtrip": RoundtripMacro.self
]

@Suite("@Roundtrip Macro Tests")
struct RoundtripMacroTests {
  // Test cases...
}
```

**Required test cases:**

1. `testJSONRoundtripExpansion` - Default .json strategy
   - Input: `@Roundtrip struct User: Codable, Equatable { ... }`
   - Verify: Generated enum with @Test function, JSONEncoder/JSONDecoder usage

2. `testPropertyListRoundtripExpansion` - .plist strategy
   - Input: `@Roundtrip(via: .plist) struct Config: Codable, Equatable { ... }`
   - Verify: PropertyListEncoder/PropertyListDecoder usage

3. `testHashStabilityExpansion` - .hash strategy
   - Input: `@Roundtrip(via: .hash) struct Point: Hashable { ... }`
   - Verify: hashValue comparison test generated

4. `testCustomIterationsExpansion` - Custom config
   - Input: `@Roundtrip(via: .json, iterations: 500, maxShrinks: 2000)`
   - Verify: Config values propagated to PropertyConfig

5. `testSeedExpansion` - Seed parameter
   - Input: `@Roundtrip(via: .json, seed: 12345)`
   - Verify: Seed passed to PropertyConfig

6. `testInvalidTargetDiagnostic` - Applied to enum
   - Input: `@Roundtrip enum Status { ... }`
   - Verify: Diagnostic error emitted

7. `testInvalidTargetProtocol` - Applied to protocol
   - Input: `@Roundtrip protocol Foo { }`
   - Verify: Diagnostic error emitted

8. `testClassSupport` - Applied to class
   - Input: `@Roundtrip class Document: Codable, Equatable { ... }`
   - Verify: Generates correctly for classes too

9. `testMissingEquatableDiagnostic` - Missing Equatable for .json
   - Input: `@Roundtrip struct Data: Codable { ... }` (no Equatable)
   - Verify: Diagnostic error about missing Equatable conformance

10. `testMissingHashableDiagnostic` - Missing Hashable for .hash
    - Input: `@Roundtrip(via: .hash) struct Point { ... }` (no Hashable)
    - Verify: Diagnostic error about missing Hashable conformance

Use `assertMacroExpansion` for all tests. Match expected output exactly including whitespace.

IMPORTANT: Use Swift Testing (@Test, @Suite), not XCTest.
  </action>
  <verify>
Tests compile: `swift build --target InvariantSwiftMacroTests 2>&1 | grep -E "error:" | head -5 || echo "Tests compile"`

Test count: `grep -c "@Test" Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift`
  </verify>
  <done>
RoundtripMacroTests.swift exists with 10+ expansion tests covering all strategies, error cases, and conformance checking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests</name>
  <files>
    Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift
  </files>
  <action>
Create integration tests that verify @Roundtrip-generated tests actually work with real types.

```swift
import Testing
import Foundation
@testable import InvariantSwift

@Suite("@Roundtrip Integration Tests")
struct RoundtripIntegrationTests {
  // Test cases...
}
```

**Required test cases:**

1. `testJSONRoundtripPasses` - Verify roundtrip succeeds for valid Codable
   - Create simple Codable struct (User with name: String, age: Int)
   - Manually invoke the roundtrip test pattern
   - Verify property passes with 100 iterations

2. `testPlistRoundtripPasses` - PropertyList roundtrip
   - Create Codable struct with plist-compatible types
   - Test with PropertyListEncoder/PropertyListDecoder

3. `testHashStabilityPasses` - Hash consistency
   - Create Hashable struct
   - Verify hashValue == hashValue for same instance

4. `testRoundtripFailsForBrokenCodable` - Detect encoding issues
   - Create type with lossy Codable implementation (e.g., date formatting issue)
   - Verify property test would catch the issue

5. `testNestedTypesRoundtrip` - Complex nested structures
   - Create struct with nested Codable types
   - Verify roundtrip handles nesting

6. `testArraysAndOptionalsRoundtrip` - Collections
   - Create struct with [Int], String?, [String]?
   - Verify all survive roundtrip

7. `testSetRoundtripWithHashable` - Set insertion/retrieval
   - Create Hashable type
   - Verify Set insertion and contains work correctly

**Helpers:**
Create helper function to run roundtrip property test:
```swift
func verifyRoundtrip<T: Codable & Equatable & Generatable>(
  _ type: T.Type,
  encoder: JSONEncoder = JSONEncoder(),
  decoder: JSONDecoder = JSONDecoder()
) async throws -> Bool
```

IMPORTANT: These are functional tests, not macro expansion tests. They verify the BEHAVIOR works.
  </action>
  <verify>
Tests compile: `swift build --target InvariantSwiftTests 2>&1 | grep -E "error:" | head -5 || echo "Tests compile"`

Test count: `grep -c "@Test" Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift`

Tests actually run and pass: `swift test --filter RoundtripIntegrationTests 2>&1 | grep -E "Test Suite.*passed|tests passed"`
  </verify>
  <done>
RoundtripIntegrationTests.swift exists with 7+ integration tests verifying actual roundtrip behavior. Tests execute successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run tests and fix any issues</name>
  <files>
    Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift
    Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift
  </files>
  <action>
Run all tests and fix any failures.

1. Run macro tests:
```bash
swift test --filter RoundtripMacroTests 2>&1 | head -50
```

2. Run integration tests:
```bash
swift test --filter RoundtripIntegrationTests 2>&1 | head -50
```

3. If expansion tests fail, compare expected vs actual output and adjust.

4. If integration tests fail, debug the property test execution.

5. Run lint check:
```bash
swiftlint lint --strict Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift
```

6. Fix any lint violations.

Common issues to watch for:
- Whitespace differences in macro expansion
- Missing imports in test files
- Generator availability for test types
- Async/await handling in property runners

Goal: All tests pass with zero lint violations.
  </action>
  <verify>
Macro tests pass: `swift test --filter RoundtripMacroTests 2>&1 | grep -E "(PASS|FAIL|error)" | tail -5`

Integration tests pass: `swift test --filter RoundtripIntegrationTests 2>&1 | grep -E "(PASS|FAIL|error)" | tail -5`

Lint clean: `swiftlint lint --strict Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift 2>&1 | grep -E "violations|error" | head -3`
  </verify>
  <done>
All @Roundtrip tests pass. Zero lint violations. Macro generates correct property tests.
  </done>
</task>

</tasks>

<verification>
1. Macro tests pass: `swift test --filter RoundtripMacroTests`
2. Integration tests pass: `swift test --filter RoundtripIntegrationTests`
3. Lint check: `swiftlint lint --strict Tests/InvariantSwiftMacroTests/RoundtripMacroTests.swift Tests/InvariantSwiftTests/RoundtripIntegrationTests.swift`
4. Test count: At least 17 tests total (10 macro + 7 integration)
5. Full build: `swift build -Xswiftc -warnings-as-errors`
</verification>

<success_criteria>
- 10+ macro expansion tests pass
- 7+ integration tests pass
- Error diagnostics tested for invalid usage
- Conformance checking tested (missing Equatable/Hashable)
- All three strategies tested (.json, .plist, .hash)
- Zero lint violations
- Zero compiler warnings
</success_criteria>

<output>
After completion, create `.planning/phases/07-roundtrip/07-02-SUMMARY.md`
</output>
