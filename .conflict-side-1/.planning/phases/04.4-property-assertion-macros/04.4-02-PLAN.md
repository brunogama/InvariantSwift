---
phase: 04.4-property-assertion-macros
plan: 02
type: execute
wave: 2
depends_on: ["04.4-01"]
files_modified:
  - Sources/InvariantSwift/Macros/PureMacroDeclaration.swift
  - Sources/InvariantSwiftMacros/PropertyAssertionMacros/PureMacro.swift
  - Sources/InvariantSwiftMacros/MacroPlugin.swift
  - Tests/InvariantSwiftMacroTests/PropertyAssertionMacroTests.swift
autonomous: true

must_haves:
  truths:
    - "@Pure generates a test verifying determinism (documented limitation: cannot detect side effects)"
    - "Comprehensive test suite covers all three macros"
    - "Tests verify macro expansion produces correct AST"
    - "Tests verify diagnostics for invalid usage"
  artifacts:
    - path: "Sources/InvariantSwift/Macros/PureMacroDeclaration.swift"
      provides: "@Pure public macro declaration"
      contains: "public macro Pure"
    - path: "Sources/InvariantSwiftMacros/PropertyAssertionMacros/PureMacro.swift"
      provides: "PureMacro PeerMacro implementation"
      contains: "struct PureMacro: PeerMacro"
    - path: "Tests/InvariantSwiftMacroTests/PropertyAssertionMacroTests.swift"
      provides: "Test suite for all three property assertion macros"
      contains: "@Suite"
  key_links:
    - from: "Sources/InvariantSwiftMacros/MacroPlugin.swift"
      to: "PureMacro.self"
      via: "providingMacros array registration"
      pattern: "PureMacro\\.self"
    - from: "Tests/InvariantSwiftMacroTests/PropertyAssertionMacroTests.swift"
      to: "assertMacroExpansion"
      via: "SwiftSyntaxMacrosTestSupport"
      pattern: "assertMacroExpansion"
---

<objective>
Implement @Pure macro and comprehensive test suite for all property assertion macros.

Purpose: Complete the property assertion macro trio with @Pure (determinism verification with documented limitations), and ensure all three macros have thorough test coverage including expansion tests and diagnostic tests.

Output: @Pure macro fully implemented, comprehensive test suite with 15+ tests covering all three macros.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.4-property-assertion-macros/04.4-RESEARCH.md
@.planning/phases/04.4-property-assertion-macros/04.4-01-SUMMARY.md

# Prior plan artifacts
@Sources/InvariantSwift/Macros/IdempotentMacroDeclaration.swift
@Sources/InvariantSwift/Macros/DeterministicMacroDeclaration.swift
@Sources/InvariantSwiftMacros/PropertyAssertionMacros/IdempotentMacro.swift
@Sources/InvariantSwiftMacros/PropertyAssertionMacros/DeterministicMacro.swift
@Sources/InvariantSwiftMacros/PropertyAssertionMacros/PropertyAssertionDiagnostics.swift

# Test patterns to follow
@Tests/InvariantSwiftMacroTests/PropertyMacroTests.swift
@Tests/InvariantSwiftMacroTests/ArbitraryMacroTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement @Pure macro</name>
  <files>
    Sources/InvariantSwift/Macros/PureMacroDeclaration.swift
    Sources/InvariantSwiftMacros/PropertyAssertionMacros/PureMacro.swift
    Sources/InvariantSwiftMacros/MacroPlugin.swift
  </files>
  <action>
**PureMacroDeclaration.swift:**
- Import InvariantSwift
- Create `@attached(peer, names: suffixed(_PureTest))` macro declaration
- Parameters: `iterations: Int = 100`, `callCount: Int = 2`
- Use `#externalMacro(module: "InvariantSwiftMacros", type: "PureMacro")`
- Add CRITICAL documentation explaining:
  - What it tests: determinism (f(x) == f(x))
  - What it CANNOT detect: side effects, state mutation
  - Swift lacks effect tracking like Haskell's type system
  - Recommend manual code review for state mutation
  - Use cases: marking functions safe for memoization, parallelization
  - Example usage

**PureMacro.swift:**
Create `public struct PureMacro: PeerMacro` reusing DeterministicMacro logic:

1. Implement expansion following DeterministicMacro pattern exactly
2. The test logic is identical (f(x) == f(x) verification)
3. Generate wrapper enum `{funcName}_PureTest`
4. Property test verifies repeated calls return same result

The difference from @Deterministic is semantic (intent/documentation), not behavioral.
Consider whether to share implementation via helper functions or keep separate for clarity.

**MacroPlugin.swift:**
- Add `PureMacro.self` to providingMacros array

**CRITICAL:** Document in the macro declaration that @Pure only verifies determinism. True purity (no side effects) cannot be verified at compile time in Swift.
  </action>
  <verify>
- `swift build` compiles successfully
- PureMacro registered in MacroPlugin.swift
- No SwiftLint violations
  </verify>
  <done>
- PureMacroDeclaration.swift exists with documented limitations
- PureMacro.swift implements determinism verification
- MacroPlugin.swift registers PureMacro
- Documentation clearly states what @Pure does and does not verify
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive test suite</name>
  <files>
    Tests/InvariantSwiftMacroTests/PropertyAssertionMacroTests.swift
  </files>
  <action>
Create a comprehensive test file using Swift Testing framework and SwiftSyntaxMacrosTestSupport.

**Test structure:**
```swift
import SwiftSyntaxMacrosTestSupport
import Testing
@testable import InvariantSwiftMacros

@Suite("Property Assertion Macros")
struct PropertyAssertionMacroTests {
  // Define testMacros dictionary with all three macros
}
```

**@Idempotent tests (5 tests):**
1. Basic expansion - single Int parameter
2. Multiple parameters - generates flatMap chain
3. Async function - generates await in closure
4. Custom applicationCount parameter
5. Diagnostic: applied to non-function

**@Deterministic tests (5 tests):**
1. Basic expansion - single parameter
2. Multiple parameters
3. Async function support
4. Custom callCount parameter
5. Diagnostic: no parameters

**@Pure tests (5 tests):**
1. Basic expansion - verifies determinism test generated
2. Multiple parameters
3. Async function support
4. Custom iterations parameter
5. Diagnostic: Void return type (document that it's allowed but may be useless)

**Test pattern to follow:**
```swift
@Test("@Idempotent generates idempotency test")
func testIdempotentBasic() {
  assertMacroExpansion(
    """
    @Idempotent
    func normalize(_ value: Int) -> Int {
      abs(value)
    }
    """,
    expandedSource: """
    func normalize(_ value: Int) -> Int {
      abs(value)
    }

    private enum normalize_IdempotentTest {
      @Test("normalize")
      static func run() throws {
        // Generated test body...
      }
    }
    """,
    macros: testMacros
  )
}
```

**Diagnostic tests:**
Use `diagnostics:` parameter in assertMacroExpansion to verify error messages.

**Important:** Tests should verify the STRUCTURE of generated code, not exact whitespace. Focus on:
- Enum name follows pattern `{funcName}_XxxTest`
- @Test attribute present with function name
- Generator inference used
- Property construction correct
- Result handling present
  </action>
  <verify>
- `swift test --filter PropertyAssertionMacroTests` passes
- At least 15 tests total
- No SwiftLint violations in test file
  </verify>
  <done>
- PropertyAssertionMacroTests.swift exists with 15+ tests
- All @Idempotent expansion tests pass
- All @Deterministic expansion tests pass
- All @Pure expansion tests pass
- Diagnostic tests verify error messages
- Zero test failures
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `swift build -Xswiftc -warnings-as-errors`
2. All tests pass: `swift test --filter PropertyAssertionMacroTests`
3. No lint violations: `swiftlint lint --strict`
4. All three macros registered in MacroPlugin.swift
</verification>

<success_criteria>
- @Pure macro declaration is public with documented limitations
- PureMacro generates determinism verification test
- Documentation clearly states @Pure cannot detect side effects
- Test suite has 15+ tests covering all three macros
- Expansion tests verify correct AST generation
- Diagnostic tests verify error handling
- All tests pass
- Zero warnings, zero lint violations
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-property-assertion-macros/04.4-02-SUMMARY.md`
</output>
