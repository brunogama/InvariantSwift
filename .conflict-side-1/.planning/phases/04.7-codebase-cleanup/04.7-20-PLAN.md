---
phase: 04.7-codebase-cleanup
plan: 20
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/InvariantSwift/Core/Generator.swift
  - Sources/InvariantSwift/Core/Property.swift
  - Sources/InvariantSwift/Core/RunReport.swift
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All blanket swiftlint:disable comments are documented with technical rationale"
    - "Generator.swift file_length disable has tech debt comment explaining why and linking to future refactoring"
    - "Property.swift and RunReport.swift use function-scoped enable/disable pairs (not blanket file-level disables)"
    - "swiftlint lint --strict passes with zero errors"
    - "swift build -Xswiftc -warnings-as-errors passes"
  artifacts:
    - path: "Sources/InvariantSwift/Core/Generator.swift"
      provides: "Generator types with documented file_length blanket disable"
      contains: "TECH DEBT comment on lines 1-4 explaining why blanket disable is necessary"
    - path: "Sources/InvariantSwift/Core/Property.swift"
      provides: "PropertyRunner with no blanket complexity/length disables"
      contains: "Function-scoped enable/disable pairs only"
    - path: "Sources/InvariantSwift/Core/RunReport.swift"
      provides: "RunReport with no blanket disables"
      contains: "Function-scoped enable/disable pairs only"
  key_links:
    - from: "swiftlint lint --strict"
      to: "Sources/InvariantSwift/Core/*.swift"
      via: "zero violations"
      pattern: "0 errors"
---

<objective>
Document and verify all SwiftLint disables are properly scoped, closing Gap 1 from verification.

Purpose: Ensure all swiftlint:disable markers follow acceptable patterns:
1. Generator.swift: file_length blanket disable is DOCUMENTED with tech debt rationale (unavoidable for file-level rules)
2. Property.swift and RunReport.swift: Use function-scoped enable/disable pairs (not blanket file-level disables)

Output:
- Generator.swift has documented file_length disable with clear rationale
- All other disables use function-scoped enable/disable pairs
- swiftlint lint --strict passes
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-codebase-cleanup/04.7-VERIFICATION.md
@Sources/InvariantSwift/Core/Generator.swift
@Sources/InvariantSwift/Core/Property.swift
@Sources/InvariantSwift/Core/RunReport.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Generator.swift file_length blanket disable</name>
  <files>Sources/InvariantSwift/Core/Generator.swift</files>
  <action>
Generator.swift has a blanket `// swiftlint:disable file_length` on line 1. The file is 1690 lines (SwiftLint limit is 400 warning, 1000 error).

**STRATEGY DECISION: Option A (Pragmatic)**

Keep the blanket disable but add clear technical rationale documentation. Splitting the file is a significant refactoring effort beyond the scope of this gap closure phase.

**Rationale for keeping blanket disable:**
- file_length is a file-level rule - there is NO way to use :next/:this/:previous syntax
- Splitting Gen/Shrink/Size/Seed into separate files requires careful API design (future ISP work)
- Documented blanket disables are acceptable when technical constraints prevent alternatives

**Implementation**:
Update lines 1-4 of Generator.swift to:
```swift
// swiftlint:disable file_length
// TECH DEBT: Generator.swift (1690 lines) exceeds 1000-line limit.
// This is unavoidable without splitting Gen/Shrink/Size/Seed into separate files.
// Future work: ISP proposal for generator modularization.
```

This is the ONLY acceptable blanket disable in the codebase. All other disables MUST use function-scoped enable/disable pairs.
  </action>
  <verify>
1. Line 1-4 of Generator.swift have documented blanket disable with TECH DEBT comment
2. `swiftlint lint --strict Sources/InvariantSwift/Core/Generator.swift` passes
3. `swift build -Xswiftc -warnings-as-errors` succeeds
  </verify>
  <done>
Generator.swift file_length disable is documented with technical rationale including: why it's unavoidable, what future work would address it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Property.swift disables are function-scoped pairs</name>
  <files>Sources/InvariantSwift/Core/Property.swift</files>
  <action>
Property.swift has these disables that should be function-scoped (NOT blanket file-level):
- `// swiftlint:disable cyclomatic_complexity` before runPropertyWithFailingExamples
- `// swiftlint:disable function_body_length` before runThrowingProperty
- `// swiftlint:disable function_body_length` before runAsyncThrowingProperty

**Verification steps:**

1. Search for each disable and confirm matching enable exists:
   ```bash
   grep -n "swiftlint:disable cyclomatic_complexity" Sources/InvariantSwift/Core/Property.swift
   grep -n "swiftlint:enable cyclomatic_complexity" Sources/InvariantSwift/Core/Property.swift
   ```

2. For each disable/enable pair, verify:
   - Disable appears BEFORE the function
   - Enable appears AFTER the function ends
   - The pair is NOT at file level (line 1)

3. If any disable lacks a matching enable, ADD the enable after the function.

**Expected outcome:**
- runPropertyWithFailingExamples: cyclomatic_complexity disable/enable pair
- runThrowingProperty: function_body_length disable/enable pair (already verified)
- runAsyncThrowingProperty: function_body_length disable/enable pair (already verified)

These are function-scoped disables (acceptable) NOT blanket file-level disables (not acceptable except for Generator.swift file_length).
  </action>
  <verify>
1. Each `// swiftlint:disable` has a matching `// swiftlint:enable` comment
2. `swiftlint lint --strict Sources/InvariantSwift/Core/Property.swift` passes
3. All disable/enable pairs are at function scope, not file scope
  </verify>
  <done>
All Property.swift disables have matching enables and are function-scoped.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify RunReport.swift has properly scoped disables</name>
  <files>Sources/InvariantSwift/Core/RunReport.swift</files>
  <action>
RunReport.swift has:
- Line 223: `// swiftlint:disable function_body_length function_parameter_count`
- Line 306: `// swiftlint:enable function_body_length function_parameter_count`

This is already a properly scoped enable/disable pair around the `buildReport` function (lines 225-305).

**Verify**:
1. The disable is on line 223
2. The enable is on line 306
3. The function `buildReport` is between lines 225-305
4. This is a function-scoped pair, not a blanket disable

If all checks pass, no changes needed. Document that this is acceptable.
  </action>
  <verify>
1. `swiftlint lint --strict Sources/InvariantSwift/Core/RunReport.swift` passes
2. Disable/enable pair properly surrounds the buildReport function
  </verify>
  <done>
RunReport.swift disables are already properly function-scoped with matching enables.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "swiftlint:disable " Sources/InvariantSwift/Core/*.swift | grep -v ":next\|:this\|:previous"` - shows only properly documented disables with matching enables
2. `swiftlint lint --strict Sources/InvariantSwift/Core/` - 0 errors
3. `swift build -Xswiftc -warnings-as-errors` - passes
4. Generator.swift line 1-4 has TECH DEBT documentation for file_length disable
5. Property.swift and RunReport.swift have ONLY function-scoped disable/enable pairs (no file-level blanket disables)

NOTE: This plan does NOT run swift test - verification is lint-only. swift test is handled by Plan 19.
</verification>

<success_criteria>
1. Generator.swift has documented file_length disable with TECH DEBT explanation and future work reference
2. Property.swift cyclomatic_complexity disable has matching enable (function-scoped)
3. Property.swift function_body_length disables have matching enables (function-scoped)
4. RunReport.swift disables are function-scoped with matching enables
5. NO blanket file-level disables exist EXCEPT Generator.swift file_length (which is documented)
6. swiftlint lint --strict passes on all Core/ files
7. swift build -Xswiftc -warnings-as-errors passes
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-20-SUMMARY.md`
</output>
