---
phase: 04.7-codebase-cleanup
plan: 17
type: execute
wave: 6
depends_on: ["04.7-16"]
gap_closure: true
files_modified:
  - Sources/InvariantSwift/Advanced/CoverageGuided.swift
  - Sources/InvariantSwiftExperimental/Exports.swift
autonomous: true

must_haves:
  truths:
    - "CoverageReport exported from InvariantSwiftExperimental"
    - "CoverageCollector exported from InvariantSwiftExperimental"
    - "Coverage types accessible in tests"
  artifacts:
    - path: "Sources/InvariantSwiftExperimental/Exports.swift"
      provides: "Public exports for coverage types"
  key_links:
    - from: "Coverage type exports"
      to: "LensSystemTests"
      via: "coverage type references"
---

<objective>
Fix Coverage type exports for InvariantSwiftExperimental (Infrastructure 4/4)

Purpose: Ensure CoverageReport, CoverageCollector, and related types are properly exported from InvariantSwiftExperimental.
Output: Coverage types accessible from test files
</objective>

<execution_context>
@/Users/bruno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bruno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-codebase-cleanup/04.7-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix Coverage type exports</name>
  <files>
    Sources/InvariantSwift/Advanced/CoverageGuided.swift
    Sources/InvariantSwiftExperimental/Exports.swift
  </files>
  <action>
Fix coverage type visibility:

1. Check current Coverage types location:
   ```bash
   rg "struct CoverageReport|actor CoverageCollector|enum CoverageStrategy" Sources/
   ```

2. Check InvariantSwiftExperimental module structure:
   ```bash
   ls -la Sources/InvariantSwiftExperimental/
   rg "import.*Advanced\|export.*Coverage" Sources/InvariantSwiftExperimental/
   ```

3. Ensure types are public:
   ```bash
   rg "public.*CoverageReport|public.*CoverageCollector" Sources/InvariantSwift/Advanced/
   ```

4. If types are in Advanced/ but not exported:
   - Check if InvariantSwiftExperimental re-exports Advanced module
   - If not, create or update Exports.swift:

   ```swift
   // Sources/InvariantSwiftExperimental/Exports.swift
   @_exported import InvariantSwift

   // Re-export coverage types if they're internal
   public typealias CoverageReport = InvariantSwift.CoverageReport
   public typealias CoverageCollector = InvariantSwift.CoverageCollector
   public typealias CoverageStrategy = InvariantSwift.CoverageStrategy
   public typealias CoverageConfig = InvariantSwift.CoverageConfig
   ```

5. Alternatively, ensure Advanced module types are public:
   ```swift
   // In Sources/InvariantSwift/Advanced/PropertyRunner+Coverage.swift
   public struct CoverageReport: Sendable { ... }
   public actor CoverageCollector { ... }
   public enum CoverageStrategy: Sendable { ... }
   ```

6. Verify imports work in tests:
   ```bash
   cd Tests/InvariantSwiftTests/FunctionalTesting/
   rg "import.*Experimental" LensSystemTests.swift
   swift build --target InvariantSwiftTests 2>&1 | grep "CoverageReport\|CoverageCollector" | head -10
   ```
  </action>
  <verify>
Run: `rg "public.*Coverage" Sources/InvariantSwift/Advanced/ | wc -l`
Expected: >= 4

Run: `swift build --target InvariantSwiftTests 2>&1 | grep -c "error.*CoverageReport\|error.*CoverageCollector"`
Expected: 0
  </verify>
  <done>Coverage types properly exported and accessible</done>
</task>

</tasks>

<verification>
Overall verification for Plan 17:

1. Coverage types public: `rg "public.*struct CoverageReport|public.*actor CoverageCollector" Sources/ | wc -l` >= 2
2. Types importable: `swift build --target InvariantSwiftTests 2>&1 | grep -c "error.*Coverage"` = 0
3. Build succeeds: `swift build --target InvariantSwiftTests 2>&1 | grep -c "error:"` <= 10 (only remaining non-coverage errors)
</verification>

<success_criteria>
- CoverageReport is public and exported
- CoverageCollector is public and exported
- CoverageStrategy is public and exported
- Tests can import and use coverage types
- Coverage-related compilation errors eliminated
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-codebase-cleanup/04.7-17-SUMMARY.md`
</output>
