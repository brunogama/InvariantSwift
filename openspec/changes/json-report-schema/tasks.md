# Tasks

- [ ] Define JSON schema v1 in spec delta (fields, types, units)
- [ ] Implement RunReport + FailureReport encoding
- [ ] Expose API to emit JSON report; optional CLI support
- [ ] Add schema snapshot tests to prevent accidental breaking changes
- [ ] Document how to consume in CI
