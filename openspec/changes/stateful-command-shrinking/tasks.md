# Tasks

- [ ] Define CommandSequence model with deterministic ordering
- [ ] Implement sequence shrinker: chunk deletions first, then command shrinking
- [ ] Make shrink search deterministic and time-bounded (if timeouts enabled)
- [ ] Add example model (e.g., Stack) and tests asserting minimal sequences
- [ ] Document stateful testing API surface
