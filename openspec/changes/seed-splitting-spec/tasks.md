# Tasks

- [ ] Define ReplayToken v1 schema (seed, sizeSchedule, iterations, shrinkTrace?)
- [ ] Implement seed splitting utility (e.g., SplitMix64) with tests
- [ ] Refactor combinators (zip/flatMap/apply) to use split RNG streams where required
- [ ] Implement encode/decode and roundtrip tests for replay tokens
- [ ] Document backward compatibility guarantees
