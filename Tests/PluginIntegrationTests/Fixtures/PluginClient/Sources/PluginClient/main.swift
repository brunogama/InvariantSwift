import InvariantSwiftCore

// PropertyResult is generic; a concrete argument is required for the
// reference to type-check. This fixture only proves the module links.
let _: PropertyResult<Int>? = nil
