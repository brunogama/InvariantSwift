// swiftlint:disable:next blanket_disable_command
// swiftlint:disable all
// swiftformat:disable all
// swift-format-ignore-file
// Generated by InvariantSwift Ghostwriter
// Source: Packages/InvariantSwiftMacros/Sources/GhostwriterLib/SwiftSyntaxTypeExtractor.swift
//
// DO NOT EDIT - Regenerate with: swift package ghostwrite

import Testing
import Foundation
import InvariantSwiftTesting
import InvariantSwiftMacroAPI
@testable import GhostwriterLib
import enum GhostwriterLib.AccessLevel

// MARK: - Auto-Generated Arbitrary Conformances

extension AccessLevel: InvariantSwiftCore.Generatable {
    public static var arbitrary: Gen<AccessLevel> {
        Gen.oneOf([Gen.pure(AccessLevel.`private`), Gen.pure(AccessLevel.`fileprivate`), Gen.pure(AccessLevel.`internal`), Gen.pure(AccessLevel.`public`), Gen.pure(AccessLevel.`open`),])
    }
}

@Suite("SwiftSyntaxTypeExtractor Property Tests")
private struct GhostwriterSuite_bdb125c2600a8770 {

  // MARK: - AccessLevel

  /// Comparable asymmetry: x < y implies !(y < x).
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_comparableAsymmetric(a: AccessLevel, b: AccessLevel) {
      if a < b {
          #expect(!(b < a), "Asymmetry: a < b implies !(b < a)")
      }
  }

  /// Comparable derived operators agree with < and ==.
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_comparableDerivedOperators(a: AccessLevel, b: AccessLevel) {
      let lessOrEqual = a <= b
      let derivedLessOrEqual = (a < b) || (a == b)
      #expect(lessOrEqual == derivedLessOrEqual, "<= must agree")
      #expect((a > b) == (b < a), "> must reverse <")
      #expect((a >= b) == (b <= a), ">= must reverse <=")
  }

  /// Comparable irreflexivity: !(x < x).
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_comparableIrreflexive(value: AccessLevel) {
      #expect(!(value < value), "Irreflexivity: !(x < x)")
  }

  /// Comparable min and max agree with ordering.
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_comparableMinMax(a: AccessLevel, b: AccessLevel) {
      #expect(min(a, b) <= a, "min must not exceed a")
      #expect(min(a, b) <= b, "min must not exceed b")
      #expect(max(a, b) >= a, "max must not be below a")
      #expect(max(a, b) >= b, "max must not be below b")
  }

  /// Comparable transitivity: x < y and y < z implies x < z.
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_comparableTransitive(a: AccessLevel, b: AccessLevel, c: AccessLevel) {
      if (a < b) && (b < c) {
          #expect(a < c, "Transitivity: a < b and b < c implies a < c")
      }
  }

  /// Comparable trichotomy: exactly one of <, ==, and > holds.
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_comparableTrichotomy(a: AccessLevel, b: AccessLevel) {
      let isLess = a < b
      let isEqual = a == b
      let isGreater = b < a
      let exactlyOne = [isLess, isEqual, isGreater].filter {
          $0
      } .count == 1
      #expect(exactlyOne, "Exactly one ordering relation must hold")
  }

  /// Equatable negation: != is the negation of ==.
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_equatableNegation(a: AccessLevel, b: AccessLevel) {
      let isUnequal = a != b
      let isEqual = a == b
      #expect(isUnequal == !isEqual, "!= must negate ==")
  }

  /// Equatable reflexivity: x == x for all non-NaN x.
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_equatableReflexive(value: AccessLevel) {
      #expect(value == value, "Reflexivity: x == x")
  }

  /// Equatable symmetry: equality agrees in both directions.
  @PropertyTest func test14_GhostwriterLib_11_AccessLevel_equatableSymmetric(a: AccessLevel, b: AccessLevel) {
      let forward = a == b
      let reverse = b == a
      #expect(forward == reverse, "Equality must be symmetric")
  }

}
